// Code generated by the Encore v1.53.8 client generator. DO NOT EDIT.

// Disable eslint, jshint, and jslint for this file.
/* eslint-disable */
/* jshint ignore:start */
/*jslint-disable*/

/**
 * BaseURL is the base URL for calling the Encore application's API.
 */
export type BaseURL = string

export const Local: BaseURL = "http://localhost:4000"

/**
 * Environment returns a BaseURL for calling the cloud environment with the given name.
 */
export function Environment(name: string): BaseURL {
    return `https://${name}-conductor-ee92.encr.app`
}

/**
 * PreviewEnv returns a BaseURL for calling the preview environment with the given PR number.
 */
export function PreviewEnv(pr: number | string): BaseURL {
    return Environment(`pr${pr}`)
}

const BROWSER = typeof globalThis === "object" && ("window" in globalThis);

/**
 * Client is an API client for the conductor-ee92 Encore application.
 */
export default class Client {
    public readonly admin: admin.ServiceClient
    public readonly attachments: attachments.ServiceClient
    public readonly comment: comment.ServiceClient
    public readonly dashboard: dashboard.ServiceClient
    public readonly email: email.ServiceClient
    public readonly invitations: invitations.ServiceClient
    public readonly marketCenters: marketCenters.ServiceClient
    public readonly notifications: notifications.ServiceClient
    public readonly reports: reports.ServiceClient
    public readonly seed: seed.ServiceClient
    public readonly settings: settings.ServiceClient
    public readonly sla: sla.ServiceClient
    public readonly subscription: subscription.ServiceClient
    public readonly surveys: surveys.ServiceClient
    public readonly ticket: ticket.ServiceClient
    public readonly todos: todos.ServiceClient
    public readonly user: user.ServiceClient
    private readonly options: ClientOptions
    private readonly target: string


    /**
     * Creates a Client for calling the public and authenticated APIs of your Encore application.
     *
     * @param target  The target which the client should be configured to use. See Local and Environment for options.
     * @param options Options for the client
     */
    constructor(target: BaseURL, options?: ClientOptions) {
        this.target = target
        this.options = options ?? {}
        const base = new BaseClient(this.target, this.options)
        this.admin = new admin.ServiceClient(base)
        this.attachments = new attachments.ServiceClient(base)
        this.comment = new comment.ServiceClient(base)
        this.dashboard = new dashboard.ServiceClient(base)
        this.email = new email.ServiceClient(base)
        this.invitations = new invitations.ServiceClient(base)
        this.marketCenters = new marketCenters.ServiceClient(base)
        this.notifications = new notifications.ServiceClient(base)
        this.reports = new reports.ServiceClient(base)
        this.seed = new seed.ServiceClient(base)
        this.settings = new settings.ServiceClient(base)
        this.sla = new sla.ServiceClient(base)
        this.subscription = new subscription.ServiceClient(base)
        this.surveys = new surveys.ServiceClient(base)
        this.ticket = new ticket.ServiceClient(base)
        this.todos = new todos.ServiceClient(base)
        this.user = new user.ServiceClient(base)
    }

    /**
     * Creates a new Encore client with the given client options set.
     *
     * @param options Client options to set. They are merged with existing options.
     **/
    public with(options: ClientOptions): Client {
        return new Client(this.target, {
            ...this.options,
            ...options,
        })
    }
}

/**
 * ClientOptions allows you to override any default behaviour within the generated Encore client.
 */
export interface ClientOptions {
    /**
     * By default the client will use the inbuilt fetch function for making the API requests.
     * however you can override it with your own implementation here if you want to run custom
     * code on each API request made or response received.
     */
    fetcher?: Fetcher

    /** Default RequestInit to be used for the client */
    requestInit?: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }

    /**
     * Allows you to set the authentication data to be used for each
     * request either by passing in a static object or by passing in
     * a function which returns a new object for each request.
     */
    auth?: auth.AuthParams | AuthDataGenerator
}

export namespace admin {
    export interface DashboardData {
        totalUsers: number
        totalOrders: number
        totalRevenue: number
    }

    export interface HealthCheck {
        status: string
        timestamp: string
    }

    export interface MigrationStatusResponse {
        migrations?: any[]
        tableCount?: number
        status: string
        error?: string
    }

    export interface ResetDatabaseRequest {
        adminSecret: string
        confirmReset: boolean
        resetType: "soft" | "hard"
    }

    export interface ResetDatabaseResponse {
        success: boolean
        message: string
        deletedCounts?: {
            comments: number
            tickets: number
            users: number
        }
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.fixMigrations = this.fixMigrations.bind(this)
            this.getDashboardData = this.getDashboardData.bind(this)
            this.health = this.health.bind(this)
            this.migrationStatus = this.migrationStatus.bind(this)
            this.softReset = this.softReset.bind(this)
        }

        /**
         * Fix migration issues
         */
        public async fixMigrations(params: {
    /**
     * Fix migration issues
     */
    adminSecret: string
}): Promise<void> {
            await this.baseClient.callTypedAPI("POST", `/admin/db/fix-migrations`, JSON.stringify(params))
        }

        /**
         * A simple authenticated API endpoint that returns some fake data
         */
        public async getDashboardData(): Promise<DashboardData> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/admin.getDashboardData`)
            return await resp.json() as DashboardData
        }

        /**
         * Health check endpoint
         */
        public async health(): Promise<HealthCheck> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/health`)
            return await resp.json() as HealthCheck
        }

        public async migrationStatus(): Promise<MigrationStatusResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/admin/db/migration-status`)
            return await resp.json() as MigrationStatusResponse
        }

        /**
         * Soft reset - deletes data but keeps structure
         */
        public async softReset(params: ResetDatabaseRequest): Promise<ResetDatabaseResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/admin/db/soft-reset`, JSON.stringify(params))
            return await resp.json() as ResetDatabaseResponse
        }
    }
}

export namespace attachments {
    export interface AttachmentInfo {
        id: string
        fileName: string
        fileSize: number
        mimeType: string
        uploadedBy: string
        uploaderName: string
        createdAt: string
    }

    export interface DeleteAttachmentResponse {
        success: boolean
    }

    export interface GetDownloadUrlResponse {
        downloadUrl: string
        fileName: string
        mimeType: string
    }

    export interface GetSignedUploadUrlRequest {
        ticketId: string
        fileName: string
        fileSize: number
        mimeType: string
    }

    export interface GetSignedUploadUrlResponse {
        uploadUrl: string
        bucketKey: string
        attachmentId: string
    }

    export interface ListAttachmentsResponse {
        attachments: AttachmentInfo[]
    }

    export interface UploadAttachmentRequest {
        ticketId: string
        fileName: string
        fileSize: number
        mimeType: string
        content: string
    }

    export interface UploadAttachmentResponse {
        attachment: {
            id: string
            fileName: string
            fileSize: number
            mimeType: string
            ticketId: string
            uploadedBy: string
            createdAt: string
            downloadUrl?: string
        }
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.deleteAttachment = this.deleteAttachment.bind(this)
            this.getDownloadUrl = this.getDownloadUrl.bind(this)
            this.getSignedUploadUrl = this.getSignedUploadUrl.bind(this)
            this.list = this.list.bind(this)
            this.upload = this.upload.bind(this)
        }

        /**
         * Delete an attachment
         */
        public async deleteAttachment(attachmentId: string): Promise<DeleteAttachmentResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/attachments/${encodeURIComponent(attachmentId)}`)
            return await resp.json() as DeleteAttachmentResponse
        }

        /**
         * Get a signed download URL for an attachment
         */
        public async getDownloadUrl(attachmentId: string): Promise<GetDownloadUrlResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/attachments/${encodeURIComponent(attachmentId)}/download-url`)
            return await resp.json() as GetDownloadUrlResponse
        }

        /**
         * Get a signed URL for direct file upload from the client
         * This is more efficient for large files as they don't need to go through the backend
         */
        public async getSignedUploadUrl(params: GetSignedUploadUrlRequest): Promise<GetSignedUploadUrlResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/attachments/signed-upload-url`, JSON.stringify(params))
            return await resp.json() as GetSignedUploadUrlResponse
        }

        /**
         * List all attachments for a ticket
         */
        public async list(ticketId: string): Promise<ListAttachmentsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/attachments/ticket/${encodeURIComponent(ticketId)}`)
            return await resp.json() as ListAttachmentsResponse
        }

        /**
         * Upload a file attachment to a ticket
         */
        public async upload(params: UploadAttachmentRequest): Promise<UploadAttachmentResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/attachments/upload`, JSON.stringify(params))
            return await resp.json() as UploadAttachmentResponse
        }
    }
}

export namespace auth {
    export interface AuthParams {
        authorization: string
    }
}

export namespace comment {
    export interface CommentCreatedEvent {
        type: "comment.created"
        comment: ticket.Comment
        ticketId: string
    }

    export interface CommentDeletedEvent {
        type: "comment.deleted"
        commentId: string
        ticketId: string
    }

    export type CommentEvent = CommentCreatedEvent | CommentUpdatedEvent | CommentDeletedEvent

    export interface CommentStreamMessage {
        event: CommentEvent
    }

    export interface CommentUpdatedEvent {
        type: "comment.updated"
        comment: ticket.Comment
        ticketId: string
    }

    export interface CreateCommentRequest {
        content: string
        internal?: boolean
    }

    export interface CreateCommentResponse {
        comment: ticket.Comment
        usersToNotify: notifications.UsersToNotify[]
        ticketTitle: string
    }

    export interface DeleteCommentResponse {
        success: boolean
        message: string
    }

    export interface ListCommentsResponse {
        comments: ticket.Comment[]
    }

    export interface UpdateCommentRequest {
        content: string
        internal?: boolean
    }

    export interface UpdateCommentResponse {
        comment: ticket.Comment
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.commentStream = this.commentStream.bind(this)
            this.create = this.create.bind(this)
            this.deleteComment = this.deleteComment.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async commentStream(ticketId: string): Promise<StreamIn<CommentStreamMessage>> {
            return await this.baseClient.createStreamIn(`/comments/stream/${encodeURIComponent(ticketId)}`)
        }

        public async create(ticketId: string, params: CreateCommentRequest): Promise<CreateCommentResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/tickets/${encodeURIComponent(ticketId)}/comments`, JSON.stringify(params))
            return await resp.json() as CreateCommentResponse
        }

        public async deleteComment(ticketId: string, commentId: string): Promise<DeleteCommentResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/tickets/${encodeURIComponent(ticketId)}/comments/${encodeURIComponent(commentId)}`)
            return await resp.json() as DeleteCommentResponse
        }

        public async list(ticketId: string): Promise<ListCommentsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/tickets/${encodeURIComponent(ticketId)}/comments`)
            return await resp.json() as ListCommentsResponse
        }

        public async update(ticketId: string, commentId: string, params: UpdateCommentRequest): Promise<UpdateCommentResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/tickets/${encodeURIComponent(ticketId)}/comments/${encodeURIComponent(commentId)}`, JSON.stringify(params))
            return await resp.json() as UpdateCommentResponse
        }
    }
}

export namespace dashboard {
    export interface DashboardMetricsResponse {
        totalTickets: number
        openTickets: number
        overdueTickets: number
        highPriorityOpen: number
        unassignedOpen: number
        createdLast7Days: number
        resolvedLast7Days: number
        avgResponseTime: number
        ticketsByStatus: {
            DRAFT: number
            CREATED: number
            ASSIGNED: number
            UNASSIGNED: number
            "AWAITING_RESPONSE": number
            "IN_PROGRESS": number
            RESOLVED: number
        }
        ticketsByUrgency: {
            HIGH: number
            MEDIUM: number
            LOW: number
        }
    }

    export interface GetMetricsRequest {
        marketCenterId?: string
    }

    export interface GetMetricsResponse {
        metrics: DashboardMetricsResponse
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.getMetrics = this.getMetrics.bind(this)
        }

        public async getMetrics(params: GetMetricsRequest): Promise<GetMetricsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                marketCenterId: params.marketCenterId,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/dashboard/metrics`, undefined, {query})
            return await resp.json() as GetMetricsResponse
        }
    }
}

export namespace email {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.inboundEmail = this.inboundEmail.bind(this)
            this.webhookHealth = this.webhookHealth.bind(this)
        }

        /**
         * Webhook endpoint for receiving emails from Resend
         */
        public async inboundEmail(method: "POST", body?: RequestInit["body"], options?: CallParameters): Promise<globalThis.Response> {
            return this.baseClient.callAPI(method, `/webhooks/email/inbound`, body, options)
        }

        /**
         * Health check endpoint for webhook
         */
        public async webhookHealth(): Promise<{
    status: string
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/webhooks/email/health`)
            return await resp.json() as {
    status: string
}
        }
    }
}

export namespace invitations {
    export interface AcceptInvitationResponse {
        success: boolean
        userId?: string
        marketCenterId?: string
        role?: user.UserRole
    }

    export interface CancelInvitationResponse {
        success: boolean
    }

    export interface GetInvitationResponse {
        invitation: {
            id: string
            name: string
            email: string
            role: user.UserRole
            status: marketCenters.InvitationStatus
            marketCenterName: string
            marketCenterId: string
            inviterName: string
            inviterEmail: string
            expiresAt: string
            isExpired: boolean
        } | null
        valid: boolean
        message?: string
    }

    export interface InviteTeamMemberRequest {
        email: string
        role: user.UserRole
        name: string
    }

    export interface InviteTeamMemberResponse {
        success: boolean
        invitationId: string
        /**
         * TODO: Remove token from response once email is confirmed working
         */
        token: string

        signupUrl: string
    }

    export interface ListInvitationsRequest {
        inviteStatus?: marketCenters.InvitationStatus
        marketCenterIds?: string
        limit?: number
        offset?: number
    }

    export interface ListInvitationsResponse {
        invitations: marketCenters.TeamInvitation[]
    }

    export interface ResendInvitationResponse {
        success: boolean
        newExpiresAt: string
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.acceptInvitation = this.acceptInvitation.bind(this)
            this.cancelInvitation = this.cancelInvitation.bind(this)
            this.getInvitation = this.getInvitation.bind(this)
            this.inviteTeamMember = this.inviteTeamMember.bind(this)
            this.listInvitations = this.listInvitations.bind(this)
            this.resendInvitation = this.resendInvitation.bind(this)
        }

        public async acceptInvitation(token: string): Promise<AcceptInvitationResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/invitations/${encodeURIComponent(token)}/accept`)
            return await resp.json() as AcceptInvitationResponse
        }

        public async cancelInvitation(token: string): Promise<CancelInvitationResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/invitations/${encodeURIComponent(token)}`)
            return await resp.json() as CancelInvitationResponse
        }

        public async getInvitation(token: string): Promise<GetInvitationResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/invitations/${encodeURIComponent(token)}`)
            return await resp.json() as GetInvitationResponse
        }

        public async inviteTeamMember(params: InviteTeamMemberRequest): Promise<InviteTeamMemberResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/invitations`, JSON.stringify(params))
            return await resp.json() as InviteTeamMemberResponse
        }

        public async listInvitations(params: ListInvitationsRequest): Promise<ListInvitationsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                inviteStatus:    params.inviteStatus === undefined ? undefined : String(params.inviteStatus),
                limit:           params.limit === undefined ? undefined : String(params.limit),
                marketCenterIds: params.marketCenterIds,
                offset:          params.offset === undefined ? undefined : String(params.offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/invitations/list`, undefined, {query})
            return await resp.json() as ListInvitationsResponse
        }

        public async resendInvitation(token: string): Promise<ResendInvitationResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/invitations/${encodeURIComponent(token)}/resend`)
            return await resp.json() as ResendInvitationResponse
        }
    }
}

export namespace marketCenters {
    export interface CreateMarketCenterRequest {
        name: string
        users?: user.User[]
        ticketCategories?: {
            name: string
            description: string
        }[]
    }

    export interface CreateMarketCenterResponse {
        marketCenter: MarketCenter
        usersToNotify?: notifications.UsersToNotify[]
    }

    export interface DeleteMarketCenterResponse {
        success: boolean
        message: string
    }

    export interface GetMarketCenterResponse {
        marketCenter: MarketCenter
    }

    export type InvitationStatus = "PENDING" | "ACCEPTED" | "EXPIRED" | "CANCELLED"

    export interface ListMarketCentersRequest {
        id?: string
        categoryIds?: string[]
        userIds?: string[]
        query?: string
        sortBy?: "updatedAt" | "createdAt" | "urgency" | "status"
        sortDir?: "asc" | "desc"
        limit?: number
        offset?: number
    }

    export interface ListMarketCentersResponse {
        marketCenters: MarketCenter[]
        total: number
    }

    export interface MarketCenter {
        id: string
        name: string
        createdAt: string
        updatedAt: string
        primaryStripeCustomerId: string | null
        primaryStripeSubscriptionId: string | null
        teamInvitations?: TeamInvitation[]
        ticketCategories?: TicketCategory[]
        users?: user.User[]
        marketCenterHistory?: MarketCenterHistory[]
        totalTickets?: number
        totalUsers?: number
        settings?: settings.MarketCenterSettings
        averages?: surveys.SurveyResults
    }

    export interface MarketCenterHistory {
        id: string
        marketCenterId: string
        action: string
        field: string
        previousValue: string | null
        newValue: string | null
        changedAt: string
        changedById: string
        marketCenter?: MarketCenter
        changedBy?: user.User
    }

    export interface RemoveUsersRequest {
        users: user.User[]
    }

    export interface RemoveUsersResponse {
        marketCenter: MarketCenter
    }

    export interface TeamInvitation {
        id: string
        name: string
        email: string
        role: user.UserRole
        status: InvitationStatus
        marketCenterId?: string
        invitedBy?: string
        token: string
        expiresAt: string
        acceptedAt?: string
        createdAt: string
        updatedAt: string
        marketCenter?: MarketCenter
    }

    export interface TicketCategory {
        id: string
        name: string
        description: string | null
        marketCenterId: string
        defaultAssigneeId?: string | null
        isActive?: boolean
        createdAt: string
        updatedAt: string
        defaultAssignee?: user.User | null
        marketCenter?: MarketCenter
        ticketCount?: number
    }

    export interface UpdateMarketCenterRequest {
        name?: string
        users?: user.User[]
        ticketCategories?: TicketCategory[]
    }

    export interface UpdateMarketCenterResponse {
        marketCenter: MarketCenter
        usersToNotify: notifications.UsersToNotify[]
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.createCategory = this.createCategory.bind(this)
            this.createMCNotificationPreferences = this.createMCNotificationPreferences.bind(this)
            this.deleteCategory = this.deleteCategory.bind(this)
            this.deleteMarketCenter = this.deleteMarketCenter.bind(this)
            this.get = this.get.bind(this)
            this.getMCNotificationPreferences = this.getMCNotificationPreferences.bind(this)
            this.getUserHistory = this.getUserHistory.bind(this)
            this.listCategories = this.listCategories.bind(this)
            this.removeUsers = this.removeUsers.bind(this)
            this.search = this.search.bind(this)
            this.update = this.update.bind(this)
            this.updateCategory = this.updateCategory.bind(this)
            this.updateMCNotificationPreferences = this.updateMCNotificationPreferences.bind(this)
        }

        public async create(params: CreateMarketCenterRequest): Promise<CreateMarketCenterResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/marketCenters/create`, JSON.stringify(params))
            return await resp.json() as CreateMarketCenterResponse
        }

        public async createCategory(params: ticketCategories.CreateCategoryRequest): Promise<ticketCategories.CreateCategoryResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/marketCenters/ticketCategories`, JSON.stringify(params))
            return await resp.json() as ticketCategories.CreateCategoryResponse
        }

        public async createMCNotificationPreferences(marketCenterId: string): Promise<notification_preferences.CreateMCNotificationPreferencesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/settings/market-center/${encodeURIComponent(marketCenterId)}`)
            return await resp.json() as notification_preferences.CreateMCNotificationPreferencesResponse
        }

        public async deleteCategory(id: string): Promise<ticketCategories.DeleteCategoryResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/marketCenters/ticketCategories/${encodeURIComponent(id)}`)
            return await resp.json() as ticketCategories.DeleteCategoryResponse
        }

        public async deleteMarketCenter(id: string): Promise<DeleteMarketCenterResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/marketCenters/${encodeURIComponent(id)}`)
            return await resp.json() as DeleteMarketCenterResponse
        }

        public async get(id: string): Promise<GetMarketCenterResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/marketCenters/${encodeURIComponent(id)}`)
            return await resp.json() as GetMarketCenterResponse
        }

        public async getMCNotificationPreferences(marketCenterId: string): Promise<notification_preferences.GetMarketCenterNotificationPreferencesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/settings/market-center/${encodeURIComponent(marketCenterId)}`)
            return await resp.json() as notification_preferences.GetMarketCenterNotificationPreferencesResponse
        }

        public async getUserHistory(id: string, params: history.GetMarketCenterHistoryRequest): Promise<history.GetMarketCenterHistoryResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:   params.limit === undefined ? undefined : String(params.limit),
                offset:  params.offset === undefined ? undefined : String(params.offset),
                orderBy: String(params.orderBy),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/marketCenter/${encodeURIComponent(id)}/history`, undefined, {query})
            return await resp.json() as history.GetMarketCenterHistoryResponse
        }

        public async listCategories(params: ticketCategories.ListCategoriesRequest): Promise<ticketCategories.ListCategoriesResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                id:             params.id,
                marketCenterId: params.marketCenterId,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/marketCenters/ticketCategories`, undefined, {query})
            return await resp.json() as ticketCategories.ListCategoriesResponse
        }

        /**
         * Removes users from a market center
         */
        public async removeUsers(id: string, params: RemoveUsersRequest): Promise<RemoveUsersResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/marketCenters/users/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as RemoveUsersResponse
        }

        public async search(params: ListMarketCentersRequest): Promise<ListMarketCentersResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                categoryIds: params.categoryIds?.map((v) => v),
                id:          params.id,
                limit:       params.limit === undefined ? undefined : String(params.limit),
                offset:      params.offset === undefined ? undefined : String(params.offset),
                query:       params.query,
                sortBy:      params.sortBy === undefined ? undefined : String(params.sortBy),
                sortDir:     params.sortDir === undefined ? undefined : String(params.sortDir),
                userIds:     params.userIds?.map((v) => v),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/marketCenters/search`, undefined, {query})
            return await resp.json() as ListMarketCentersResponse
        }

        /**
         * Creates a new market center
         */
        public async update(id: string, params: UpdateMarketCenterRequest): Promise<UpdateMarketCenterResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/marketCenters/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as UpdateMarketCenterResponse
        }

        public async updateCategory(id: string, params: ticketCategories.UpdateCategoryRequest): Promise<ticketCategories.UpdateCategoryResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/marketCenters/ticketCategories/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as ticketCategories.UpdateCategoryResponse
        }

        public async updateMCNotificationPreferences(marketCenterId: string, params: notification_preferences.UpdateMCNotificationPreferencesRequest): Promise<notification_preferences.UpdateMCNotificationPreferencesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/settings/market-center/${encodeURIComponent(marketCenterId)}`, JSON.stringify(params))
            return await resp.json() as notification_preferences.UpdateMCNotificationPreferencesResponse
        }
    }
}

export namespace notifications {
    export interface CreateNotificationPayload {
        userId: string
        templateName?: string
        category: NotificationCategory
        type: string
        email: {
            title?: string
            body?: string
        } | "Notifications deactivated"
        inApp: {
            title?: string
            body?: string
        } | "Notifications deactivated"
        data?: NotificationData
        priority?: ticket.Urgency
    }

    export interface CreateNotificationRequest {
        templateName?: string
        category: NotificationCategory
        type: string
        email: {
            title: string
            body: string
        } | "Notifications deactivated"
        inApp: {
            title: string
            body: string
        } | "Notifications deactivated"
        data?: NotificationData
        priority?: ticket.Urgency
    }

    export interface DeleteNotificationsRequest {
        notificationIds?: string[]
    }

    export interface DeleteNotificationsResponse {
        deletedCount: number
        message: string
    }

    export interface ListInAppNotificationsRequest {
        limit?: number
        offset?: number
    }

    export interface ListInAppNotificationsResponse {
        notifications: Notification[]
        unReadAmount: number
    }

    export interface MarketCenterDefaultTemplates {
        marketCenterId: string
        notificationTemplateId: string
        createdAt: string
        marketCenter?: marketCenters.MarketCenter
        notificationTemplate?: NotificationTemplate
    }

    export interface Notification {
        id: string
        userId: string
        user?: user.User
        channel?: NotificationChannel
        category: NotificationCategory
        priority?: ticket.Urgency
        type: string
        title: string
        body: string
        data?: NotificationData
        read: boolean
        deliveredAt: string | null
        createdAt: string
    }

    export type NotificationCategory = "ACCOUNT" | "ACTIVITY" | "MARKETING" | "PERMISSIONS" | "PRODUCT"

    export type NotificationChannel = "EMAIL" | "PUSH" | "IN_APP" | "SMS"

    export interface NotificationContent {
        /**
         * getToken: GetToken;
         */
        templateName: string

        trigger: "App Permissions" | "Invitation" | "Account Information" | "Ticket Created" | "Ticket Updated" | "Ticket Assignment" | "Mentions" | "New Comments" | "Market Center Assignment" | "Category Assignment" | "Ticket Survey" | "Ticket Survey Results"
        receivingUser: {
            id: string
            name: string
            email: string
        }
        data?: NotificationData
    }

    export interface NotificationData {
        /**
         * IN-APP OR BROWSER
         */
        url?: string

        ticketId?: string
        marketCenterId?: string
        userId?: string
        commentId?: string
        categoryId?: string
        /**
         * EMAIL
         */
        appPermissions?: emails.AppPermissionsReviewProps

        accountInformation?: emails.AccountInformationProps
        emails?: string[]
        invitation?: emails.NewUserInvitationProps
        /**
         * ACTIVITY: MARKET CENTER
         */
        marketCenterAssignment?: emails.MarketCenterAssignmentProps

        categoryAssignment?: emails.CategoryAssignmentProps
        /**
         * ACTIVITY: TICKETS
         */
        createdTicket?: emails.CreatedTicketNotificationProps

        updatedTicket?: emails.UpdatedTicketProps
        ticketAssignment?: emails.AssignedTicketNotificationProps
        newComment?: emails.NewCommentNotificationProps
        ticketSurvey?: emails.TicketSurveyProps
        surveyResults?: emails.SurveyResultsProps
        /**
         * SLA
         */
        slaEventType?: "WARNING_50" | "WARNING_75" | "BREACHED" | "MET" | "RESOLUTION_WARNING_50" | "RESOLUTION_WARNING_75" | "RESOLUTION_BREACHED"

        slaType?: "response" | "resolution"
        urgency?: ticket.Urgency
        assigneeId?: string | null
    }

    export type NotificationFrequency = "NONE" | "INSTANT" | "DAILY" | "WEEKLY" | "MONTHLY" | "QUARTERLY" | "ANNUALLY"

    export interface NotificationTemplate {
        id: string
        templateName: string
        templateDescription: string
        category: NotificationCategory
        channel: NotificationChannel
        type?: string
        subject: string
        body: string
        isDefault: boolean
        createdAt: string
        variables: any
        isActive: boolean
        marketCenterId: string | null
        marketCenter?: marketCenters.MarketCenter
        marketCenterDefaultTemplates?: MarketCenterDefaultTemplates[]
    }

    export interface UpdateNotificationResponse {
        success: boolean
    }

    export interface UsersToNotify {
        id: string
        name: string
        email: string
        updateType: emails.AssignmentUpdateType
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteNotifications = this.deleteNotifications.bind(this)
            this.format = this.format.bind(this)
            this.formatStandard = this.formatStandard.bind(this)
            this.get = this.get.bind(this)
            this.getDefaultTemplates = this.getDefaultTemplates.bind(this)
            this.getTemplateForEditing = this.getTemplateForEditing.bind(this)
            this.getTemplateVariables = this.getTemplateVariables.bind(this)
            this.listInApp = this.listInApp.bind(this)
            this.listTemplateStatuses = this.listTemplateStatuses.bind(this)
            this.listTemplates = this.listTemplates.bind(this)
            this.notificationStream = this.notificationStream.bind(this)
            this.previewEmailTemplate = this.previewEmailTemplate.bind(this)
            this.previewInAppTemplate = this.previewInAppTemplate.bind(this)
            this.resetEmailTemplate = this.resetEmailTemplate.bind(this)
            this.resetInAppTemplate = this.resetInAppTemplate.bind(this)
            this.resetNotificationTemplate = this.resetNotificationTemplate.bind(this)
            this.saveEmailTemplate = this.saveEmailTemplate.bind(this)
            this.saveInAppTemplate = this.saveInAppTemplate.bind(this)
            this.update = this.update.bind(this)
            this.updateNotificationTemplate = this.updateNotificationTemplate.bind(this)
        }

        public async create(userId: string, params: CreateNotificationRequest): Promise<void> {
            await this.baseClient.callTypedAPI("POST", `/notifications/create/${encodeURIComponent(userId)}`, JSON.stringify(params))
        }

        public async deleteNotifications(userId: string, params: DeleteNotificationsRequest): Promise<DeleteNotificationsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                notificationIds: params.notificationIds?.map((v) => v),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/notifications/${encodeURIComponent(userId)}`, undefined, {query})
            return await resp.json() as DeleteNotificationsResponse
        }

        public async format(templateName: string, params: templates.FormatNotificationRequest): Promise<templates.FormatNotificationResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notifications/templates/format/${encodeURIComponent(templateName)}`, JSON.stringify(params))
            return await resp.json() as templates.FormatNotificationResponse
        }

        public async formatStandard(templateName: string, params: templates.FormatNotificationRequest): Promise<templates.FormatNotificationResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notifications/templates/format/standard/${encodeURIComponent(templateName)}`, JSON.stringify(params))
            return await resp.json() as templates.FormatNotificationResponse
        }

        public async get(id: string): Promise<templates.GetNotificationTemplateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/notifications/templates/${encodeURIComponent(id)}`)
            return await resp.json() as templates.GetNotificationTemplateResponse
        }

        /**
         * Returns the system default templates for a template type.
         * Used when resetting to defaults or for reference.
         */
        public async getDefaultTemplates(templateType: string): Promise<customizations.GetDefaultTemplatesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/notifications/template-customizations/defaults/${encodeURIComponent(templateType)}`)
            return await resp.json() as customizations.GetDefaultTemplatesResponse
        }

        /**
         * Gets a specific template type with all data needed for the editing UI.
         * Includes defaults, current customizations (if any), and available variables/fields.
         */
        public async getTemplateForEditing(marketCenterId: string, templateType: string): Promise<customizations.GetTemplateForEditingResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/notifications/template-customizations/market-center/${encodeURIComponent(marketCenterId)}/template/${encodeURIComponent(templateType)}`)
            return await resp.json() as customizations.GetTemplateForEditingResponse
        }

        /**
         * Returns the available variables for a template type.
         * Used by the frontend to populate the variable picker.
         */
        public async getTemplateVariables(templateType: string): Promise<customizations.GetTemplateVariablesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/notifications/template-customizations/variables/${encodeURIComponent(templateType)}`)
            return await resp.json() as customizations.GetTemplateVariablesResponse
        }

        public async listInApp(email: string, params: ListInAppNotificationsRequest): Promise<ListInAppNotificationsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:  params.limit === undefined ? undefined : String(params.limit),
                offset: params.offset === undefined ? undefined : String(params.offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/notifications/in-app/${encodeURIComponent(email)}`, undefined, {query})
            return await resp.json() as ListInAppNotificationsResponse
        }

        /**
         * Lists all template types with their customization status for a market center.
         * Shows whether each template type is using default or custom content.
         */
        public async listTemplateStatuses(marketCenterId: string): Promise<customizations.ListTemplateStatusesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/notifications/template-customizations/market-center/${encodeURIComponent(marketCenterId)}`)
            return await resp.json() as customizations.ListTemplateStatusesResponse
        }

        public async listTemplates(params: templates.ListNotificationTemplatesRequest): Promise<templates.ListNotificationTemplatesResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                templateName: params.templateName,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/notifications/templates`, undefined, {query})
            return await resp.json() as templates.ListNotificationTemplatesResponse
        }

        public async notificationStream(): Promise<StreamIn<Notification>> {
            return await this.baseClient.createStreamIn(`/notifications/stream`)
        }

        /**
         * Generates a preview of an email template with sample data.
         * Does not save anything - just shows what the email would look like.
         */
        public async previewEmailTemplate(params: customizations.PreviewEmailTemplateRequest): Promise<customizations.PreviewEmailTemplateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notifications/template-customizations/email/preview`, JSON.stringify(params))
            return await resp.json() as customizations.PreviewEmailTemplateResponse
        }

        /**
         * Generates a preview of an in-app notification with sample data.
         * Does not save anything - just shows what the notification would look like.
         */
        public async previewInAppTemplate(params: customizations.PreviewInAppTemplateRequest): Promise<customizations.PreviewInAppTemplateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notifications/template-customizations/in-app/preview`, JSON.stringify(params))
            return await resp.json() as customizations.PreviewInAppTemplateResponse
        }

        /**
         * Deletes an email template customization, reverting to the system default.
         */
        public async resetEmailTemplate(marketCenterId: string, templateType: string): Promise<customizations.ResetEmailTemplateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/notifications/template-customizations/email/${encodeURIComponent(marketCenterId)}/${encodeURIComponent(templateType)}`)
            return await resp.json() as customizations.ResetEmailTemplateResponse
        }

        /**
         * Deletes an in-app template customization, reverting to the system default.
         */
        public async resetInAppTemplate(marketCenterId: string, templateType: string): Promise<customizations.ResetInAppTemplateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/notifications/template-customizations/in-app/${encodeURIComponent(marketCenterId)}/${encodeURIComponent(templateType)}`)
            return await resp.json() as customizations.ResetInAppTemplateResponse
        }

        public async resetNotificationTemplate(templateId: string): Promise<templates.ResetNotificationTemplateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notifications/templates/reset/${encodeURIComponent(templateId)}`)
            return await resp.json() as templates.ResetNotificationTemplateResponse
        }

        /**
         * Creates or updates an email template customization.
         * If a customization already exists, it will be updated.
         * If not, a new one will be created.
         */
        public async saveEmailTemplate(params: customizations.SaveEmailTemplateRequest): Promise<customizations.SaveEmailTemplateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notifications/template-customizations/email`, JSON.stringify(params))
            return await resp.json() as customizations.SaveEmailTemplateResponse
        }

        /**
         * Creates or updates an in-app template customization.
         * If a customization already exists, it will be updated.
         * If not, a new one will be created.
         */
        public async saveInAppTemplate(params: customizations.SaveInAppTemplateRequest): Promise<customizations.SaveInAppTemplateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notifications/template-customizations/in-app`, JSON.stringify(params))
            return await resp.json() as customizations.SaveInAppTemplateResponse
        }

        public async update(notificationId: string, email: string): Promise<UpdateNotificationResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/notifications/${encodeURIComponent(notificationId)}/${encodeURIComponent(email)}`)
            return await resp.json() as UpdateNotificationResponse
        }

        public async updateNotificationTemplate(params: templates.UpdateNotificationTemplateRequest): Promise<templates.UpdateNotificationTemplateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/notifications/templates/update`, JSON.stringify(params))
            return await resp.json() as templates.UpdateNotificationTemplateResponse
        }
    }
}

export namespace reports {
    export interface BacklogRequest {
        marketCenterIds?: string[]
        status?: ticket.TicketStatus[]
        categoryIds?: string[]
        dateFrom?: string
        dateTo?: string
    }

    export interface BacklogResponse {
        created: number
        unassigned: number
        total: number
    }

    export interface CreatedVolumeRequest {
        marketCenterIds?: string[]
        creatorIds?: string[]
        assigneeIds?: string[]
        categoryIds?: string[]
        dateFrom?: string
        dateTo?: string
        granularity?: Granularity
    }

    export interface CreatedVolumeResponse {
        ticketsCreated: {
            period: string
            createdCount: number
        }[]
        total: number
        granularity: Granularity
    }

    export type Granularity = "daily" | "weekly" | "monthly"

    export type Granularity = "daily" | "weekly" | "monthly"

    export interface ResolvedVolumeRequest {
        marketCenterIds?: string[]
        creatorIds?: string[]
        assigneeIds?: string[]
        categoryIds?: string[]
        dateFrom?: string
        dateTo?: string
        granularity?: Granularity
    }

    export interface ResolvedVolumeResponse {
        ticketsResolved: {
            period: string
            resolvedCount: number
        }[]
        total: number
        granularity: Granularity
    }

    export interface SLARequest {
        marketCenterIds?: string[]
        status?: ticket.TicketStatus[]
        categoryIds?: string[]
        dateFrom?: string
        dateTo?: string
    }

    export interface SLAResponse {
        response: {
            compliant: number
            onTrack: number
            atRisk: number
            overdue: number
        }
        resolve: {
            compliant: number
            onTrack: number
            atRisk: number
            overdue: number
        }
    }

    export interface TicketReview {
        id: string
        ticketId: string
        ticketTitle: string
        surveyorName: string
        assigneeName: string | null
        marketCenterName: string | null
        overallRating: number | null
        assigneeRating: number | null
        marketCenterRating: number | null
        comment: string | null
        completedAt: string
    }

    export interface TicketReviewsRequest {
        marketCenterIds?: string[]
        categoryIds?: string[]
        dateFrom?: string
        dateTo?: string
        assigneeIds?: string[]
    }

    export interface TicketReviewsResponse {
        reviews: TicketReview[]
        totalReviews: number
        averageOverallRating: number | null
        averageAssigneeRating: number | null
        averageMarketCenterRating: number | null
    }

    export interface UserSLAStats {
        id: string
        name: string
        ticketTotal: number
        response: {
            atRisk: number
            overdue: number
        }
        resolve: {
            atRisk: number
            overdue: number
        }
    }

    export interface UsersSLARequest {
        marketCenterIds?: string[]
        assigneeIds?: string[]
        status?: ticket.TicketStatus[]
        categoryIds?: string[]
        dateFrom?: string
        dateTo?: string
    }

    export interface UsersSLAResponse {
        assignees: UserSLAStats[]
        ticketTotal: number
        assigneeTotal: number
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.backlog = this.backlog.bind(this)
            this.createdByMonth = this.createdByMonth.bind(this)
            this.resolvedByMonth = this.resolvedByMonth.bind(this)
            this.slaCompliance = this.slaCompliance.bind(this)
            this.slaComplianceByUsers = this.slaComplianceByUsers.bind(this)
            this.ticketReviews = this.ticketReviews.bind(this)
        }

        public async backlog(params: BacklogRequest): Promise<BacklogResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                categoryIds:     params.categoryIds?.map((v) => v),
                dateFrom:        params.dateFrom,
                dateTo:          params.dateTo,
                marketCenterIds: params.marketCenterIds?.map((v) => v),
                status:          params.status?.map((v) => v === undefined ? undefined : String(v)),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/reports/ticket-backlog`, undefined, {query})
            return await resp.json() as BacklogResponse
        }

        public async createdByMonth(params: CreatedVolumeRequest): Promise<CreatedVolumeResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                assigneeIds:     params.assigneeIds?.map((v) => v),
                categoryIds:     params.categoryIds?.map((v) => v),
                creatorIds:      params.creatorIds?.map((v) => v),
                dateFrom:        params.dateFrom,
                dateTo:          params.dateTo,
                granularity:     params.granularity === undefined ? undefined : String(params.granularity),
                marketCenterIds: params.marketCenterIds?.map((v) => v),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/reports/created-by-month`, undefined, {query})
            return await resp.json() as CreatedVolumeResponse
        }

        public async resolvedByMonth(params: ResolvedVolumeRequest): Promise<ResolvedVolumeResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                assigneeIds:     params.assigneeIds?.map((v) => v),
                categoryIds:     params.categoryIds?.map((v) => v),
                creatorIds:      params.creatorIds?.map((v) => v),
                dateFrom:        params.dateFrom,
                dateTo:          params.dateTo,
                granularity:     params.granularity === undefined ? undefined : String(params.granularity),
                marketCenterIds: params.marketCenterIds?.map((v) => v),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/reports/resolved-by-month`, undefined, {query})
            return await resp.json() as ResolvedVolumeResponse
        }

        public async slaCompliance(params: SLARequest): Promise<SLAResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                categoryIds:     params.categoryIds?.map((v) => v),
                dateFrom:        params.dateFrom,
                dateTo:          params.dateTo,
                marketCenterIds: params.marketCenterIds?.map((v) => v),
                status:          params.status?.map((v) => v === undefined ? undefined : String(v)),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/reports/sla-compliance`, undefined, {query})
            return await resp.json() as SLAResponse
        }

        public async slaComplianceByUsers(params: UsersSLARequest): Promise<UsersSLAResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                assigneeIds:     params.assigneeIds?.map((v) => v),
                categoryIds:     params.categoryIds?.map((v) => v),
                dateFrom:        params.dateFrom,
                dateTo:          params.dateTo,
                marketCenterIds: params.marketCenterIds?.map((v) => v),
                status:          params.status?.map((v) => v === undefined ? undefined : String(v)),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/reports/sla-compliance-by-users`, undefined, {query})
            return await resp.json() as UsersSLAResponse
        }

        public async ticketReviews(params: TicketReviewsRequest): Promise<TicketReviewsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                assigneeIds:     params.assigneeIds?.map((v) => v),
                categoryIds:     params.categoryIds?.map((v) => v),
                dateFrom:        params.dateFrom,
                dateTo:          params.dateTo,
                marketCenterIds: params.marketCenterIds?.map((v) => v),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/reports/ticket-reviews`, undefined, {query})
            return await resp.json() as TicketReviewsResponse
        }
    }
}

export namespace seed {
    export interface SeedResponse {
        message: string
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.seedData = this.seedData.bind(this)
        }

        public async seedData(): Promise<SeedResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/seed`)
            return await resp.json() as SeedResponse
        }
    }
}

export namespace settings {
    export interface AutoCloseSettings {
        enabled: boolean
        awaitingResponseDays: number
    }

    export interface BrandingSettings {
        primaryColor: string
        logoUrl?: string
        companyName?: string
    }

    export interface BusinessHours {
        monday: {
            start: string
            end: string
            isOpen: boolean
        }
        tuesday: {
            start: string
            end: string
            isOpen: boolean
        }
        wednesday: {
            start: string
            end: string
            isOpen: boolean
        }
        thursday: {
            start: string
            end: string
            isOpen: boolean
        }
        friday: {
            start: string
            end: string
            isOpen: boolean
        }
        saturday: {
            start: string
            end: string
            isOpen: boolean
        }
        sunday: {
            start: string
            end: string
            isOpen: boolean
        }
    }

    export interface CategoryResponse {
        id: string
        name: string
        description?: string
        marketCenterId: string
        defaultAssigneeId?: string
        isActive: boolean
        createdAt: string
        updatedAt: string
    }

    export interface CreateNotificationsResponse {
        created: boolean
    }

    export interface CreateSettingsCategoryRequest {
        name: string
        description?: string
        defaultAssigneeId?: string
    }

    export interface CreateSettingsCategoryResponse {
        id: string
        name: string
        description?: string
        marketCenterId: string
        defaultAssigneeId?: string
        isActive: boolean
        createdAt: string
        updatedAt: string
    }

    export interface DeleteSettingsCategoryResponse {
        success: boolean
    }

    export interface GetAutoCloseSettingsResponse {
        autoClose: AutoCloseSettings
    }

    export interface GetSponsorDisplayResponse {
        slots: SponsorDisplaySlot[]
    }

    export interface GetSponsorSlotsResponse {
        sponsorSlots: SponsorSlotsWithUrls
    }

    export interface GetUserSettingsResponse {
        settings: user.UserSettings
    }

    export interface ListSettingsCategoriesResponse {
        categories: CategoryResponse[]
    }

    export interface MarketCenterNotificationPreferences {
        category: notifications.NotificationCategory
        type: string
        email: boolean
        push: boolean
        inApp: boolean
        sms: boolean
    }

    export interface MarketCenterSettings {
        notificationPreferences?: MarketCenterNotificationPreferences[]
        notificationTemplates?: notifications.NotificationTemplate[]
        ticketTemplates?: templates.TicketTemplate[]
        marketCenter?: marketCenters.MarketCenter
        autoClose?: AutoCloseSettings
        sponsorSlots?: SponsorSlots
        businessHours?: BusinessHours
        branding?: BrandingSettings
        holidays?: string[]
        integrations?: {
            apiKeys: { [key: string]: string }
            webhooks: {
                url: string
                events: string[]
            }[]
        }
        general?: {
            name: string
            timezone: string
            language: string
            autoAssignment: boolean
        }
        teamMembers?: number
    }

    export interface SponsorDisplaySlot {
        slot: SponsorSlotKey
        name?: string
        imageUrl: string
        linkUrl?: string
    }

    export interface SponsorSlot {
        enabled: boolean
        name?: string
        imageKey?: string
        linkUrl?: string
    }

    export type SponsorSlotKey = "header" | "dashboardCard" | "ticketListRow"

    export interface SponsorSlotWithUrl {
        imageUrl?: string
        enabled: boolean
        name?: string
        imageKey?: string
        linkUrl?: string
    }

    export interface SponsorSlots {
        header?: SponsorSlot
        dashboardCard?: SponsorSlot
        ticketListRow?: SponsorSlot
    }

    export interface SponsorSlotsWithUrls {
        header?: SponsorSlotWithUrl
        dashboardCard?: SponsorSlotWithUrl
        ticketListRow?: SponsorSlotWithUrl
    }

    export interface UpdateAutoCloseSettingsRequest {
        enabled: boolean
        awaitingResponseDays?: number
    }

    export interface UpdateAutoCloseSettingsResponse {
        autoClose: AutoCloseSettings
    }

    export interface UpdateSettingsCategoryRequest {
        name?: string
        description?: string
        defaultAssigneeId?: string
        isActive?: boolean
    }

    export interface UpdateSettingsCategoryResponse {
        id: string
        name: string
        description?: string
        marketCenterId: string
        defaultAssigneeId?: string
        isActive: boolean
        createdAt: string
        updatedAt: string
    }

    export interface UpdateSponsorSlotsRequest {
        sponsorSlots: SponsorSlots
    }

    export interface UpdateSponsorSlotsResponse {
        sponsorSlots: SponsorSlots
    }

    export interface UploadSponsorAssetRequest {
        slot: SponsorSlotKey
        fileName: string
        mimeType: string
        content: string
    }

    export interface UploadSponsorAssetResponse {
        imageKey: string
        imageUrl: string
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.createSettingsCategory = this.createSettingsCategory.bind(this)
            this.deleteSettingsCategory = this.deleteSettingsCategory.bind(this)
            this.getAutoCloseSettings = this.getAutoCloseSettings.bind(this)
            this.getSponsorDisplay = this.getSponsorDisplay.bind(this)
            this.getSponsorSlots = this.getSponsorSlots.bind(this)
            this.getUserHistory = this.getUserHistory.bind(this)
            this.listSettingsCategories = this.listSettingsCategories.bind(this)
            this.updateAutoCloseSettings = this.updateAutoCloseSettings.bind(this)
            this.updateSettingsCategory = this.updateSettingsCategory.bind(this)
            this.updateSponsorSlots = this.updateSponsorSlots.bind(this)
            this.uploadSponsorAsset = this.uploadSponsorAsset.bind(this)
        }

        public async createSettingsCategory(params: CreateSettingsCategoryRequest): Promise<CreateSettingsCategoryResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/settings/categories`, JSON.stringify(params))
            return await resp.json() as CreateSettingsCategoryResponse
        }

        public async deleteSettingsCategory(id: string): Promise<DeleteSettingsCategoryResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/settings/categories/${encodeURIComponent(id)}`)
            return await resp.json() as DeleteSettingsCategoryResponse
        }

        /**
         * Get auto-close settings for a market center
         */
        public async getAutoCloseSettings(marketCenterId: string): Promise<GetAutoCloseSettingsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/settings/auto-close/${encodeURIComponent(marketCenterId)}`)
            return await resp.json() as GetAutoCloseSettingsResponse
        }

        /**
         * Get the sponsor slots to display for the current user's market center.
         * Available to any authenticated role. Never throws for a missing market
         * center, missing settings, or a user without a market center - it simply
         * returns an empty list.
         */
        public async getSponsorDisplay(): Promise<GetSponsorDisplayResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/settings/partner-display`)
            return await resp.json() as GetSponsorDisplayResponse
        }

        /**
         * Get sponsor slot settings for a market center, with resolved public
         * image URLs for any configured slot.
         */
        public async getSponsorSlots(marketCenterId: string): Promise<GetSponsorSlotsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/settings/partner-slots/${encodeURIComponent(marketCenterId)}`)
            return await resp.json() as GetSponsorSlotsResponse
        }

        public async getUserHistory(userId: string, id: string): Promise<void> {
            await this.baseClient.callTypedAPI("GET", `/settings/users/${encodeURIComponent(userId)}/${encodeURIComponent(id)}`)
        }

        public async listSettingsCategories(): Promise<ListSettingsCategoriesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/settings/categories`)
            return await resp.json() as ListSettingsCategoriesResponse
        }

        /**
         * Update auto-close settings for a market center
         * Only STAFF_LEADER and ADMIN can modify these settings
         */
        public async updateAutoCloseSettings(marketCenterId: string, params: UpdateAutoCloseSettingsRequest): Promise<UpdateAutoCloseSettingsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/settings/auto-close/${encodeURIComponent(marketCenterId)}`, JSON.stringify(params))
            return await resp.json() as UpdateAutoCloseSettingsResponse
        }

        public async updateSettingsCategory(id: string, params: UpdateSettingsCategoryRequest): Promise<UpdateSettingsCategoryResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/settings/categories/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as UpdateSettingsCategoryResponse
        }

        /**
         * Update sponsor slot settings for a market center.
         * Only STAFF_LEADER and ADMIN can modify these settings.
         */
        public async updateSponsorSlots(marketCenterId: string, params: UpdateSponsorSlotsRequest): Promise<UpdateSponsorSlotsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/settings/partner-slots/${encodeURIComponent(marketCenterId)}`, JSON.stringify(params))
            return await resp.json() as UpdateSponsorSlotsResponse
        }

        /**
         * Upload a sponsor creative asset (image) for a market center's sponsor
         * slot. Only STAFF_LEADER and ADMIN can upload sponsor assets.
         */
        public async uploadSponsorAsset(marketCenterId: string, params: UploadSponsorAssetRequest): Promise<UploadSponsorAssetResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/settings/partner-slots/${encodeURIComponent(marketCenterId)}/asset`, JSON.stringify(params))
            return await resp.json() as UploadSponsorAssetResponse
        }
    }
}

export namespace sla {
    export interface ExportReportRequest {
        dateFrom?: string
        dateTo?: string
    }

    export interface ExportReportResponse {
        csv: string
        filename: string
    }

    export interface GetMetricsRequest {
        dateFrom?: string
        dateTo?: string
        assigneeId?: string
        categoryId?: string
    }

    export interface GetMetricsResponse {
        metrics: SlaMetrics
    }

    export interface GetPoliciesResponse {
        policies: SlaPolicyResponse[]
    }

    export interface GetReportRequest {
        dateFrom?: string
        dateTo?: string
        groupBy?: "day" | "week" | "month"
    }

    export interface ResolutionSlaMetrics {
        totalTickets: number
        ticketsWithSla: number
        ticketsMet: number
        ticketsBreached: number
        complianceRate: number
        avgResolutionTimeMinutes: number | null
    }

    export interface SlaMetrics {
        totalTickets: number
        ticketsWithSla: number
        ticketsMet: number
        ticketsBreached: number
        complianceRate: number
        avgResponseTimeMinutes: number | null
    }

    export interface SlaMetricsByAssignee {
        assigneeId: string | null
        assigneeName: string | null
        totalTickets: number
        ticketsMet: number
        ticketsBreached: number
        complianceRate: number
        avgResponseTimeMinutes: number | null
    }

    export interface SlaMetricsByUrgency {
        urgency: ticket.Urgency
        totalTickets: number
        ticketsMet: number
        ticketsBreached: number
        complianceRate: number
    }

    export interface SlaPolicyResponse {
        id: string
        urgency: ticket.Urgency
        responseTimeMinutes: number
        resolutionTimeMinutes: number
        isActive: boolean
        resolutionIsActive: boolean
        createdAt: string
        updatedAt: string
    }

    export interface SlaReportResponse {
        /**
         * Response SLA
         */
        metrics: SlaMetrics

        byUrgency: SlaMetricsByUrgency[]
        byAssignee: SlaMetricsByAssignee[]
        trends: SlaTrend[]
        /**
         * Resolution SLA
         */
        resolutionMetrics: ResolutionSlaMetrics

        resolutionByUrgency: SlaMetricsByUrgency[]
        resolutionTrends: SlaTrend[]
    }

    export interface SlaTrend {
        period: string
        totalTickets: number
        ticketsMet: number
        ticketsBreached: number
        complianceRate: number
    }

    export interface UpdatePolicyResponse {
        policy: SlaPolicyResponse
    }

    export interface UpdateSlaPolicyRequest {
        responseTimeMinutes?: number
        resolutionTimeMinutes?: number
        isActive?: boolean
        resolutionIsActive?: boolean
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.exportReport = this.exportReport.bind(this)
            this.getMetrics = this.getMetrics.bind(this)
            this.getPolicies = this.getPolicies.bind(this)
            this.getReport = this.getReport.bind(this)
            this.updatePolicy = this.updatePolicy.bind(this)
        }

        /**
         * Export SLA report as CSV
         */
        public async exportReport(params: ExportReportRequest): Promise<ExportReportResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                dateFrom: params.dateFrom,
                dateTo:   params.dateTo,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/sla/reports/export`, undefined, {query})
            return await resp.json() as ExportReportResponse
        }

        /**
         * Get SLA metrics summary
         */
        public async getMetrics(params: GetMetricsRequest): Promise<GetMetricsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                assigneeId: params.assigneeId,
                categoryId: params.categoryId,
                dateFrom:   params.dateFrom,
                dateTo:     params.dateTo,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/sla/metrics`, undefined, {query})
            return await resp.json() as GetMetricsResponse
        }

        /**
         * Get all SLA policies
         */
        public async getPolicies(): Promise<GetPoliciesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/sla/policies`)
            return await resp.json() as GetPoliciesResponse
        }

        /**
         * Get full SLA report with breakdown by urgency, assignee, and trends
         */
        public async getReport(params: GetReportRequest): Promise<SlaReportResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                dateFrom: params.dateFrom,
                dateTo:   params.dateTo,
                groupBy:  params.groupBy === undefined ? undefined : String(params.groupBy),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/sla/reports`, undefined, {query})
            return await resp.json() as SlaReportResponse
        }

        /**
         * Update an SLA policy
         */
        public async updatePolicy(id: string, params: UpdateSlaPolicyRequest): Promise<UpdatePolicyResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/sla/policies/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as UpdatePolicyResponse
        }
    }
}

export namespace subscription {
    export interface CreateCheckoutSessionParams {
        planType: "EARLY_BIRD" | "STANDARD"
        additionalSeats?: number
        organizationName?: string
    }

    export interface CreateCheckoutSessionResponse {
        url: string
    }

    export interface CreatePortalSessionResponse {
        url: string
    }

    export interface GetSubscriptionResponse {
        id: string
        status: SubscriptionStatus
        planType: SubscriptionPlan
        includedSeats: number
        additionalSeats: number
        totalSeats: number
        usedSeats: number
        agentCount: number
        currentPeriodStart: string
        currentPeriodEnd: string
        cancelAt: string | null
        trialEnd: string | null
        features: any
    }

    export type SubscriptionPlan = "EARLY_BIRD" | "STANDARD" | "ENTERPRISE"

    export type SubscriptionStatus = "ACTIVE" | "CANCELED" | "INCOMPLETE" | "INCOMPLETE_EXPIRED" | "PAST_DUE" | "PAUSED" | "TRIALING" | "UNPAID"

    export interface UpdateSeatsParams {
        additionalSeats: number
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.createCheckoutSession = this.createCheckoutSession.bind(this)
            this.createPortalSession = this.createPortalSession.bind(this)
            this.getSubscription = this.getSubscription.bind(this)
            this.updateSeats = this.updateSeats.bind(this)
            this.webhookHandler = this.webhookHandler.bind(this)
        }

        /**
         * Create a Stripe checkout session for new subscriptions
         */
        public async createCheckoutSession(params: CreateCheckoutSessionParams): Promise<CreateCheckoutSessionResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/subscription/checkout`, JSON.stringify(params))
            return await resp.json() as CreateCheckoutSessionResponse
        }

        /**
         * Create a Stripe customer portal session for managing subscriptions
         */
        public async createPortalSession(): Promise<CreatePortalSessionResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/subscription/portal`)
            return await resp.json() as CreatePortalSessionResponse
        }

        /**
         * Get current subscription details
         */
        public async getSubscription(): Promise<GetSubscriptionResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/subscription/current`)
            return await resp.json() as GetSubscriptionResponse
        }

        /**
         * Update additional seats for the subscription
         */
        public async updateSeats(params: UpdateSeatsParams): Promise<{
    success: boolean
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/subscription/seats`, JSON.stringify(params))
            return await resp.json() as {
    success: boolean
}
        }

        /**
         * Handle Stripe webhook events
         */
        public async webhookHandler(method: "POST", body?: RequestInit["body"], options?: CallParameters): Promise<globalThis.Response> {
            return this.baseClient.callAPI(method, `/stripe/webhook`, body, options)
        }
    }
}

export namespace surveys {
    export interface CreateSurveyRequest {
        surveyorId: string
        marketCenterId?: string
    }

    export interface CreateSurveyResponse {
        success: boolean
        usersToNotify: notifications.UsersToNotify[]
    }

    export interface DeleteSurveyResponse {
        success: boolean
    }

    export interface GetSurveyResponse {
        survey: Survey
    }

    export interface Survey {
        id: string
        completed: boolean
        ticketId: string
        surveyorId: string
        assigneeId?: string | null
        marketCenterId: string | null
        overallRating: number | null
        assigneeRating: number | null
        marketCenterRating: number | null
        comment: string | null
        createdAt: string
        updatedAt?: string
        ticket?: ticket.Ticket
        surveyor?: user.User | {
            id: string
            name: string
            email: string
        }
        assignee?: user.User | {
            id: string
            name: string
            email: string
        }
        marketCenter?: marketCenters.MarketCenter | {
            id: string
            name: string
        }
    }

    export interface SurveyResults {
        totalSurveys?: number
        overallAverageRating?: number
        assigneeAverageRating?: number
        marketCenterAverageRating?: number
    }

    export interface UpdateSurveyRequest {
        overallRating: number
        assigneeRating: number
        marketCenterRating: number
        comments?: string
    }

    export interface UpdateSurveyResponse {
        success: boolean
        usersToNotify: notifications.UsersToNotify[]
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.createSurvey = this.createSurvey.bind(this)
            this.deleteSurvey = this.deleteSurvey.bind(this)
            this.get = this.get.bind(this)
            this.getAllRatings = this.getAllRatings.bind(this)
            this.getByMarketCenter = this.getByMarketCenter.bind(this)
            this.getRatingsByAssignee = this.getRatingsByAssignee.bind(this)
            this.update = this.update.bind(this)
        }

        public async createSurvey(ticketId: string, params: CreateSurveyRequest): Promise<CreateSurveyResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/surveys/${encodeURIComponent(ticketId)}/create`, JSON.stringify(params))
            return await resp.json() as CreateSurveyResponse
        }

        public async deleteSurvey(ticketId: string): Promise<DeleteSurveyResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/ticket/surveys/${encodeURIComponent(ticketId)}`)
            return await resp.json() as DeleteSurveyResponse
        }

        public async get(surveyId: string): Promise<GetSurveyResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/surveys/${encodeURIComponent(surveyId)}`)
            return await resp.json() as GetSurveyResponse
        }

        public async getAllRatings(): Promise<SurveyResults> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/surveys/ratings/all`)
            return await resp.json() as SurveyResults
        }

        public async getByMarketCenter(marketCenterId: string): Promise<SurveyResults> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/surveys/ratings/byMarketCenter/${encodeURIComponent(marketCenterId)}`)
            return await resp.json() as SurveyResults
        }

        public async getRatingsByAssignee(assigneeId: string): Promise<SurveyResults> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/surveys/ratings/byAssignee/${encodeURIComponent(assigneeId)}`)
            return await resp.json() as SurveyResults
        }

        public async update(ticketId: string, params: UpdateSurveyRequest): Promise<UpdateSurveyResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/ticket/surveys/${encodeURIComponent(ticketId)}`, JSON.stringify(params))
            return await resp.json() as UpdateSurveyResponse
        }
    }
}

export namespace ticket {
    export interface AssignTicketRequest {
        assigneeId: string
    }

    export interface AssignTicketResponse {
        ticket: Ticket
        usersToNotify: notifications.UsersToNotify[]
    }

    export interface AssignmentRule {
        id: string
        name: string
        description: string
        conditions: RuleConditions
        action: RuleAction
        priority: number
        isActive: boolean
        createdAt: string
        updatedAt: string
    }

    export interface Attachment {
        id: string
        fileName: string
        fileSize: number
        mimeType: string
        bucketKey: string
        ticketId: string
        uploadedBy: string
        createdAt: string
        updatedAt: string
        uploader?: user.User | {
            id: string
            name: string
            email: string
        }
        uploaderName?: string
    }

    export interface BulkAssignRequest {
        ticketIds: string[]
        assigneeId: string
    }

    export interface BulkAssignResponse {
        updated: number
        failed: string[]
    }

    export interface BulkUpdateRequest {
        ticketIds: string[]
        status?: TicketStatus
        urgency?: Urgency
        category?: string
        dueDate?: string
    }

    export interface BulkUpdateResponse {
        updated: number
        failed: string[]
    }

    export interface CloseTicketRequest {
        status?: TicketStatus
    }

    export interface CloseTicketResponse {
        usersToNotify: notifications.UsersToNotify[]
    }

    export interface Comment {
        id: string
        content: string
        ticketId: string
        userId: string
        internal: boolean
        source: string
        metadata: any
        createdAt: string
        updatedAt?: string
        user?: user.User
    }

    export interface CreateRuleRequest {
        name: string
        description: string
        conditions: RuleConditions
        action: RuleAction
        priority?: number
        isActive?: boolean
    }

    export interface CreateRuleResponse {
        rule: AssignmentRule
    }

    export interface CreateTicketRequest {
        title: string
        description: string
        categoryId: string
        urgency: Urgency
        dueDate?: string
        assigneeId?: string
        todos?: string[]
    }

    export interface CreateTicketResponse {
        ticket: Ticket
        usersToNotify: notifications.UsersToNotify[]
    }

    export interface DeleteTicketResponse {
        success: boolean
        message: string
    }

    export interface GetTicketHistoryRequest {
        orderBy: string
        limit?: number
        offset?: number
    }

    export interface GetTicketHistoryResponse {
        ticketHistory: TicketHistory[]
        total: number
    }

    export interface GetTicketResponse {
        ticket: Ticket
        commentCount: number
        attachmentCount: number
    }

    export interface ListTicketAssigneesRequest {
        marketCenterId?: string
    }

    export interface ListTicketAssigneesResponse {
        assignees: TicketAssignee[]
    }

    export interface ListTicketsRequest {
        status?: TicketStatus[]
        urgency?: Urgency[]
        assigneeId?: string
        creatorId?: string
        categoryId?: string
        search?: string
        marketCenterId?: string
        limit?: number
        offset?: number
    }

    export interface ListTicketsResponse {
        tickets: Ticket[]
        total: number
    }

    export interface ReopenTicketResponse {
        usersToNotify: notifications.UsersToNotify[]
    }

    export interface RuleAction {
        assignToUserId?: string
        assignToRole?: user.UserRole
        assignToNextAvailable?: boolean
        roundRobin?: {
            userIds: string[]
            lastAssignedIndex?: number
        }
    }

    export interface RuleConditions {
        category?: string[]
        urgency?: Urgency[]
        keywords?: string[]
        creatorRole?: user.UserRole[]
        timeOfDay?: {
            /**
             * Assign based on time
             */
            start: string

            end: string
        }
        dayOfWeek?: number[]
    }

    export interface SearchTicketsRequest {
        query?: string
        status?: TicketStatus[]
        urgency?: Urgency[]
        assigneeId?: string
        creatorId?: string
        categoryId?: string
        categoryIdsMultiple?: string[]
        marketCenterId?: string
        dateFrom?: string
        dateTo?: string
        sortBy?: "updatedAt" | "createdAt" | "urgency" | "status"
        sortDir?: "asc" | "desc"
        limit?: number
        offset?: number
    }

    export interface SearchTicketsResponse {
        tickets: {
            id?: string
            title?: string | null
            description?: string | null
            status?: TicketStatus
            urgency?: Urgency
            categoryId?: string | null
            creatorId?: string
            assigneeId?: string | null
            dueDate?: string | null
            createdAt?: string
            updatedAt?: string
            creator?: user.User
            assignee?: user.User | null
            category?: marketCenters.TicketCategory | null
            commentCount?: number | null
            attachmentCount?: number | null
            attachments?: Attachment[]
            deletedAt?: string | null
            isActive?: boolean
            ticketHistory?: TicketHistory[]
            emailMessageId?: string | null
            todos?: todos.Todo[]
            surveyId?: string | null
            survey?: surveys.Survey | null
            slaResponseDueAt?: string | null
            firstResponseAt?: string | null
            slaBreached?: boolean
            slaWarning50Sent?: boolean
            slaWarning75Sent?: boolean
            slaPolicyId?: string | null
            slaPolicy?: SlaPolicy | null
            slaResolutionDueAt?: string | null
            resolvedAt?: string | null
            slaResolutionBreached?: boolean
            slaResolutionWarning50Sent?: boolean
            slaResolutionWarning75Sent?: boolean
        }[]
        total: number
    }

    export interface SlaPolicy {
        id: string
        urgency: Urgency
        responseTimeMinutes: number
        resolutionTimeMinutes: number
        isActive: boolean
        resolutionIsActive: boolean
        createdAt: string
        updatedAt: string
    }

    export interface Ticket {
        id: string
        title: string | null
        description: string | null
        status: TicketStatus
        urgency: Urgency
        categoryId?: string | null
        creatorId?: string
        assigneeId?: string | null
        dueDate: string | null
        createdAt: string
        updatedAt: string
        creator?: user.User
        assignee?: user.User | null
        category?: marketCenters.TicketCategory | null
        commentCount?: number | null
        attachmentCount?: number | null
        attachments?: Attachment[]
        deletedAt?: string | null
        isActive?: boolean
        ticketHistory?: TicketHistory[]
        emailMessageId?: string | null
        todos?: todos.Todo[]
        surveyId?: string | null
        survey?: surveys.Survey | null
        /**
         * Response SLA fields
         */
        slaResponseDueAt?: string | null

        firstResponseAt?: string | null
        slaBreached?: boolean
        slaWarning50Sent?: boolean
        slaWarning75Sent?: boolean
        slaPolicyId?: string | null
        slaPolicy?: SlaPolicy | null
        /**
         * Resolution SLA fields
         */
        slaResolutionDueAt?: string | null

        resolvedAt?: string | null
        slaResolutionBreached?: boolean
        slaResolutionWarning50Sent?: boolean
        slaResolutionWarning75Sent?: boolean
    }

    export interface TicketAssignee {
        id: string
        name: string | null
        role: string
        isActive: boolean
    }

    export interface TicketHistory {
        id: string
        ticketId: string
        action: string
        field: string | null
        previousValue: string | null
        newValue: string | null
        snapshot?: {
        } | null
        changedAt: string
        changedById: string
        changedBy?: user.User
    }

    export type TicketStatus = "DRAFT" | "CREATED" | "ASSIGNED" | "UNASSIGNED" | "AWAITING_RESPONSE" | "IN_PROGRESS" | "RESOLVED"

    export interface UpdateTicketRequest {
        title?: string
        description?: string
        status?: TicketStatus
        urgency?: Urgency
        categoryId?: string
        dueDate?: string
        assigneeId?: string
        creatorId?: string
        todos?: string[]
    }

    export interface UpdateTicketResponse {
        ticket: Ticket
        usersToNotify: notifications.UsersToNotify[]
        changedDetails: emails.ActivityUpdates[]
    }

    export type Urgency = "HIGH" | "MEDIUM" | "LOW"

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.assign = this.assign.bind(this)
            this.bulkAssign = this.bulkAssign.bind(this)
            this.bulkUpdate = this.bulkUpdate.bind(this)
            this.closeTicket = this.closeTicket.bind(this)
            this.create = this.create.bind(this)
            this.createRule = this.createRule.bind(this)
            this.createTemplate = this.createTemplate.bind(this)
            this.deleteTemplate = this.deleteTemplate.bind(this)
            this.deleteTicket = this.deleteTicket.bind(this)
            this.generateTemplates = this.generateTemplates.bind(this)
            this.get = this.get.bind(this)
            this.getTemplateById = this.getTemplateById.bind(this)
            this.getTemplates = this.getTemplates.bind(this)
            this.getTicketHistory = this.getTicketHistory.bind(this)
            this.list = this.list.bind(this)
            this.listTicketAssignees = this.listTicketAssignees.bind(this)
            this.reopen = this.reopen.bind(this)
            this.search = this.search.bind(this)
            this.update = this.update.bind(this)
            this.updateTemplate = this.updateTemplate.bind(this)
        }

        /**
         * Assigns a ticket to a user
         */
        public async assign(id: string, params: AssignTicketRequest): Promise<AssignTicketResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/tickets/${encodeURIComponent(id)}/assign`, JSON.stringify(params))
            return await resp.json() as AssignTicketResponse
        }

        public async bulkAssign(params: BulkAssignRequest): Promise<BulkAssignResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/tickets/bulk-assign`, JSON.stringify(params))
            return await resp.json() as BulkAssignResponse
        }

        public async bulkUpdate(params: BulkUpdateRequest): Promise<BulkUpdateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/tickets/bulk-update`, JSON.stringify(params))
            return await resp.json() as BulkUpdateResponse
        }

        public async closeTicket(ticketId: string, params: CloseTicketRequest): Promise<CloseTicketResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/tickets/close/${encodeURIComponent(ticketId)}`, JSON.stringify(params))
            return await resp.json() as CloseTicketResponse
        }

        public async create(params: CreateTicketRequest): Promise<CreateTicketResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/tickets`, JSON.stringify(params))
            return await resp.json() as CreateTicketResponse
        }

        public async createRule(params: CreateRuleRequest): Promise<CreateRuleResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/auto-assignments/rules`, JSON.stringify(params))
            return await resp.json() as CreateRuleResponse
        }

        public async createTemplate(marketCenterId: string, params: templates.GetTemplatesRequest): Promise<templates.GetTemplatesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/ticket-templates/${encodeURIComponent(marketCenterId)}`, JSON.stringify(params))
            return await resp.json() as templates.GetTemplatesResponse
        }

        public async deleteTemplate(templateId: string): Promise<templates.DeleteTemplatesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/ticket-templates/delete/${encodeURIComponent(templateId)}`)
            return await resp.json() as templates.DeleteTemplatesResponse
        }

        public async deleteTicket(ticketId: string): Promise<DeleteTicketResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/tickets/${encodeURIComponent(ticketId)}`)
            return await resp.json() as DeleteTicketResponse
        }

        public async generateTemplates(marketCenterId: string): Promise<templates.GetTemplatesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/ticket-templates/generate/${encodeURIComponent(marketCenterId)}`)
            return await resp.json() as templates.GetTemplatesResponse
        }

        public async get(ticketId: string): Promise<GetTicketResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/tickets/${encodeURIComponent(ticketId)}`)
            return await resp.json() as GetTicketResponse
        }

        public async getTemplateById(templateId: string): Promise<templates.TicketTemplate> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/ticket-templates/template/${encodeURIComponent(templateId)}`)
            return await resp.json() as templates.TicketTemplate
        }

        public async getTemplates(marketCenterId: string): Promise<templates.GetTemplatesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/ticket-templates/${encodeURIComponent(marketCenterId)}`)
            return await resp.json() as templates.GetTemplatesResponse
        }

        public async getTicketHistory(id: string, params: GetTicketHistoryRequest): Promise<GetTicketHistoryResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:   params.limit === undefined ? undefined : String(params.limit),
                offset:  params.offset === undefined ? undefined : String(params.offset),
                orderBy: params.orderBy,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/tickets/${encodeURIComponent(id)}/history`, undefined, {query})
            return await resp.json() as GetTicketHistoryResponse
        }

        public async list(params: ListTicketsRequest): Promise<ListTicketsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                assigneeId:     params.assigneeId,
                categoryId:     params.categoryId,
                creatorId:      params.creatorId,
                limit:          params.limit === undefined ? undefined : String(params.limit),
                marketCenterId: params.marketCenterId,
                offset:         params.offset === undefined ? undefined : String(params.offset),
                search:         params.search,
                status:         params.status?.map((v) => v === undefined ? undefined : String(v)),
                urgency:        params.urgency?.map((v) => v === undefined ? undefined : String(v)),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/tickets`, undefined, {query})
            return await resp.json() as ListTicketsResponse
        }

        /**
         * Returns the distinct set of users who are the assignee of at least one
         * ticket within the caller's visible ticket scope. This mirrors the exact
         * role-based scoping used by the main ticket list (`/tickets/search`) so
         * that a user assigned to a ticket always appears in the assignee filter,
         * even if they've since been deactivated, switched to the AGENT role, or
         * left the market center.
         */
        public async listTicketAssignees(params: ListTicketAssigneesRequest): Promise<ListTicketAssigneesResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                marketCenterId: params.marketCenterId,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/tickets/assignees`, undefined, {query})
            return await resp.json() as ListTicketAssigneesResponse
        }

        public async reopen(ticketId: string): Promise<ReopenTicketResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/tickets/reopen/${encodeURIComponent(ticketId)}`)
            return await resp.json() as ReopenTicketResponse
        }

        public async search(params: SearchTicketsRequest): Promise<SearchTicketsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                assigneeId:          params.assigneeId,
                categoryId:          params.categoryId,
                categoryIdsMultiple: params.categoryIdsMultiple?.map((v) => v),
                creatorId:           params.creatorId,
                dateFrom:            params.dateFrom,
                dateTo:              params.dateTo,
                limit:               params.limit === undefined ? undefined : String(params.limit),
                marketCenterId:      params.marketCenterId,
                offset:              params.offset === undefined ? undefined : String(params.offset),
                query:               params.query,
                sortBy:              params.sortBy === undefined ? undefined : String(params.sortBy),
                sortDir:             params.sortDir === undefined ? undefined : String(params.sortDir),
                status:              params.status?.map((v) => v === undefined ? undefined : String(v)),
                urgency:             params.urgency?.map((v) => v === undefined ? undefined : String(v)),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/tickets/search`, undefined, {query})
            return await resp.json() as SearchTicketsResponse
        }

        public async update(ticketId: string, params: UpdateTicketRequest): Promise<UpdateTicketResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/tickets/update/${encodeURIComponent(ticketId)}`, JSON.stringify(params))
            return await resp.json() as UpdateTicketResponse
        }

        public async updateTemplate(templateId: string, params: templates.UpdateTemplateRequest): Promise<templates.TicketTemplate> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/ticket-templates/template/${encodeURIComponent(templateId)}`, JSON.stringify(params))
            return await resp.json() as templates.TicketTemplate
        }
    }
}

export namespace todos {
    export interface CreateTodoRequest {
        title: string
        completed?: boolean
    }

    export interface CreateTodoResponse {
        todo: Todo
    }

    export interface DeleteTodoResponse {
        success: boolean
    }

    export interface ListTodosResponse {
        todos: Todo[]
    }

    export interface Todo {
        id: string
        title: string
        complete: boolean
        ticketId: string
        createdById: string
        updatedById?: string | null
        createdAt: string
        updatedAt?: string
        ticket?: ticket.Ticket
        createdBy?: {
            id: string
            name?: string
        } | user.User
        updatedBy?: {
            id: string
            name?: string
        } | user.User
    }

    export interface UpdateTodoRequest {
        title?: string
        completed?: boolean
    }

    export interface UpdateTodoResponse {
        success: boolean
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteTask = this.deleteTask.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(ticketId: string, params: CreateTodoRequest): Promise<CreateTodoResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/tickets/${encodeURIComponent(ticketId)}/todos`, JSON.stringify(params))
            return await resp.json() as CreateTodoResponse
        }

        public async deleteTask(ticketId: string, todoId: string): Promise<DeleteTodoResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/tickets/${encodeURIComponent(ticketId)}/todos/${encodeURIComponent(todoId)}`)
            return await resp.json() as DeleteTodoResponse
        }

        public async list(ticketId: string): Promise<ListTodosResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/tickets/${encodeURIComponent(ticketId)}/todos`)
            return await resp.json() as ListTodosResponse
        }

        public async update(ticketId: string, todoId: string, params: UpdateTodoRequest): Promise<UpdateTodoResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/tickets/${encodeURIComponent(ticketId)}/todos/${encodeURIComponent(todoId)}`, JSON.stringify(params))
            return await resp.json() as UpdateTodoResponse
        }
    }
}

export namespace user {
    export interface CreateUserRequest {
        email: string
        name: string
        role?: UserRole
        clerkId: string
        marketCenterId?: string
    }

    export interface CreateUserResponse {
        user?: User
        success: boolean
    }

    export interface DeleteUserResponse {
        success: boolean
        message: string
    }

    export interface GetCurrentUserResponse {
        id: string
        email: string
        name: string
        role: string
        isActive: boolean
        isSuperuser: boolean
        marketCenterId: string | null
        marketCenter?: {
            id: string
            name: string
        } | null
        marketCenters?: {
            id: string
            name: string
        }[]
    }

    export interface GetUserByEmailResponse {
        user: User
    }

    export interface GetUserResponse {
        user: User
        resolvedTicketsCount: number
    }

    export interface ListUsersRequest {
        role?: UserRole[]
        isActive?: boolean
    }

    export interface ListUsersResponse {
        users: User[]
    }

    export interface NotificationPreferences {
        id: string
        frequency: notifications.NotificationFrequency
        category: notifications.NotificationCategory
        type: string
        email: boolean
        push: boolean
        inApp: boolean
        sms: boolean
        userSettingsId: string
        userSettings?: UserSettings
    }

    export interface SearchUsersRequest {
        query?: string
        role?: UserRole[]
        isActive?: boolean
        marketCenterId?: string
        hasTickets?: boolean
        createdAt?: string
        updatedAt?: string
        deletedAt?: string
        sortBy?: "updatedAt" | "createdAt" | "name" | "role"
        sortDir?: "asc" | "desc"
        limit?: number
        offset?: number
    }

    export interface SearchUsersResponse {
        users: User[]
        total: number
    }

    export interface SwitchMarketCenterRequest {
        marketCenterId: string
    }

    export interface SwitchMarketCenterResponse {
        marketCenterId: string
        marketCenter: {
            id: string
            name: string
        }
    }

    export interface UpdateUserRequest {
        firstName?: string
        lastName?: string
        role?: UserRole
        isActive?: boolean
        email?: string
        marketCenterId?: string
    }

    export interface UpdateUserResponse {
        user: User
    }

    export interface User {
        id: string
        clerkId: string
        /**
         * auth0Id: string;
         */
        email: string

        name: string | null
        role: UserRole
        /**
         * staffLeader: boolean;
         */
        createdAt: string

        updatedAt: string
        isActive: boolean
        isSuperuser?: boolean
        comments?: ticket.Comment[]
        defaultForCategories?: marketCenters.TicketCategory[]
        assignedTickets?: ticket.Ticket[]
        createdTickets?: ticket.Ticket[]
        marketCenterId: string | null
        marketCenter?: marketCenters.MarketCenter
        ticketHistory?: ticket.TicketHistory[]
        userHistory?: UserHistory[]
        otherUsersChanges?: UserHistory[]
        marketCenterHistory?: marketCenters.MarketCenterHistory[]
        userSettings?: UserSettings
        notifications?: notifications.Notification[]
        "_count"?: {
            assignedTickets?: number
            createdTickets?: number
            comments?: number
            defaultForCategories?: number
        }
        responseSurveys?: surveys.Survey[]
        receivedSurveys?: surveys.Survey[]
    }

    export interface UserHistory {
        id: string
        userId: string
        marketCenterId: string | null
        action: string
        field: string | null
        previousValue: string | null
        newValue: string | null
        snapshot?: {
        } | null
        changedAt: string
        changedById: string | null
        changedBy?: {
            id: string
            name: string
            email: string
        }
        user?: {
            id: string
            name: string
            email: string
        }
    }

    export type UserRole = "AGENT" | "STAFF" | "STAFF_LEADER" | "ADMIN"

    export interface UserSettings {
        id: string
        userId: string
        notificationPreferences?: NotificationPreferences[]
        createdAt: string
        updatedAt: string
        user?: User
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.createNotifications = this.createNotifications.bind(this)
            this.deleteUser = this.deleteUser.bind(this)
            this.get = this.get.bind(this)
            this.getByEmail = this.getByEmail.bind(this)
            this.getUserHistory = this.getUserHistory.bind(this)
            this.getUserSettings = this.getUserSettings.bind(this)
            this.getUserTicketHistory = this.getUserTicketHistory.bind(this)
            this.list = this.list.bind(this)
            this.me = this.me.bind(this)
            this.resetNotificationPreferences = this.resetNotificationPreferences.bind(this)
            this.search = this.search.bind(this)
            this.switchMarketCenter = this.switchMarketCenter.bind(this)
            this.update = this.update.bind(this)
            this.updateNotificationPreferences = this.updateNotificationPreferences.bind(this)
        }

        public async create(params: CreateUserRequest): Promise<CreateUserResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/users`, JSON.stringify(params))
            return await resp.json() as CreateUserResponse
        }

        public async createNotifications(id: string): Promise<settings.CreateNotificationsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/users/${encodeURIComponent(id)}/settings`)
            return await resp.json() as settings.CreateNotificationsResponse
        }

        public async deleteUser(id: string): Promise<DeleteUserResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/users/${encodeURIComponent(id)}`)
            return await resp.json() as DeleteUserResponse
        }

        public async get(id: string): Promise<GetUserResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/users/${encodeURIComponent(id)}`)
            return await resp.json() as GetUserResponse
        }

        public async getByEmail(email: string): Promise<GetUserByEmailResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/users/email/${encodeURIComponent(email)}`)
            return await resp.json() as GetUserByEmailResponse
        }

        public async getUserHistory(id: string, params: history.GetUserHistoryRequest): Promise<history.GetUserHistoryResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:   params.limit === undefined ? undefined : String(params.limit),
                offset:  params.offset === undefined ? undefined : String(params.offset),
                orderBy: String(params.orderBy),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/users/${encodeURIComponent(id)}/history`, undefined, {query})
            return await resp.json() as history.GetUserHistoryResponse
        }

        public async getUserSettings(id: string): Promise<settings.GetUserSettingsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/users/${encodeURIComponent(id)}/settings`)
            return await resp.json() as settings.GetUserSettingsResponse
        }

        public async getUserTicketHistory(id: string, params: history.GetUserTicketHistoryRequest): Promise<history.GetUserTicketHistoryResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:   params.limit === undefined ? undefined : String(params.limit),
                offset:  params.offset === undefined ? undefined : String(params.offset),
                orderBy: params.orderBy,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/users/${encodeURIComponent(id)}/history/tickets`, undefined, {query})
            return await resp.json() as history.GetUserTicketHistoryResponse
        }

        public async list(params: ListUsersRequest): Promise<ListUsersResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                isActive: params.isActive === undefined ? undefined : String(params.isActive),
                role:     params.role?.map((v) => v === undefined ? undefined : String(v)),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/users`, undefined, {query})
            return await resp.json() as ListUsersResponse
        }

        public async me(): Promise<GetCurrentUserResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/users/me`)
            return await resp.json() as GetCurrentUserResponse
        }

        public async resetNotificationPreferences(id: string): Promise<notificationPreferences.ResetNotificationsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/users/${encodeURIComponent(id)}/settings/notificationPreferences/reset`)
            return await resp.json() as notificationPreferences.ResetNotificationsResponse
        }

        public async search(params: SearchUsersRequest): Promise<SearchUsersResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                createdAt:      params.createdAt,
                deletedAt:      params.deletedAt,
                hasTickets:     params.hasTickets === undefined ? undefined : String(params.hasTickets),
                isActive:       params.isActive === undefined ? undefined : String(params.isActive),
                limit:          params.limit === undefined ? undefined : String(params.limit),
                marketCenterId: params.marketCenterId,
                offset:         params.offset === undefined ? undefined : String(params.offset),
                query:          params.query,
                role:           params.role?.map((v) => v === undefined ? undefined : String(v)),
                sortBy:         params.sortBy === undefined ? undefined : String(params.sortBy),
                sortDir:        params.sortDir === undefined ? undefined : String(params.sortDir),
                updatedAt:      params.updatedAt,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/users/search`, undefined, {query})
            return await resp.json() as SearchUsersResponse
        }

        public async switchMarketCenter(params: SwitchMarketCenterRequest): Promise<SwitchMarketCenterResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/users/me/switch-market-center`, JSON.stringify(params))
            return await resp.json() as SwitchMarketCenterResponse
        }

        public async update(id: string, params: UpdateUserRequest): Promise<UpdateUserResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/users/${encodeURIComponent(id)}/update`, JSON.stringify(params))
            return await resp.json() as UpdateUserResponse
        }

        public async updateNotificationPreferences(id: string, params: notificationPreferences.UpdateNotificationsRequest): Promise<notificationPreferences.UpdateNotificationsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/users/${encodeURIComponent(id)}/update/settings/notifications`, JSON.stringify(params))
            return await resp.json() as notificationPreferences.UpdateNotificationsResponse
        }
    }
}

export namespace customizations {
    export interface GetDefaultTemplatesResponse {
        emailDefault: {
            subject: string
            greeting: string
            mainMessage: string
            buttonText: string
            visibleFields: string[]
        }
        inAppDefault: {
            title: string
            body: string
        }
    }

    export interface GetTemplateForEditingResponse {
        template: templates.TemplateWithDefaults
    }

    export interface GetTemplateVariablesResponse {
        variables: {
            key: string
            label: string
            description: string
            example: string
            insertText: string
        }[]
    }

    export interface ListTemplateStatusesResponse {
        templates: templates.TemplateStatus[]
    }

    export interface PreviewEmailTemplateRequest {
        marketCenterId: string
        templateType: templates.CustomizableTemplateType
        subject: string
        greeting: string
        mainMessage: string
        buttonText?: string | null
        visibleFields: string[]
    }

    export interface PreviewEmailTemplateResponse {
        preview: {
            subject: string
            greeting: string
            mainMessage: string
            buttonText: string | null
            visibleFieldsData: {
                label: string
                value: string
            }[]
        }
    }

    export interface PreviewInAppTemplateRequest {
        marketCenterId: string
        templateType: templates.CustomizableTemplateType
        title: string
        body: string
    }

    export interface PreviewInAppTemplateResponse {
        preview: {
            title: string
            body: string
        }
    }

    export interface ResetEmailTemplateResponse {
        success: boolean
    }

    export interface ResetInAppTemplateResponse {
        success: boolean
    }

    export interface SaveEmailTemplateRequest {
        marketCenterId: string
        templateType: templates.CustomizableTemplateType
        subject: string
        greeting: string
        mainMessage: string
        buttonText?: string | null
        visibleFields: string[]
    }

    export interface SaveEmailTemplateResponse {
        emailCustomization: templates.EmailTemplateCustomization
    }

    export interface SaveInAppTemplateRequest {
        marketCenterId: string
        templateType: templates.CustomizableTemplateType
        title: string
        body: string
    }

    export interface SaveInAppTemplateResponse {
        inAppCustomization: templates.InAppTemplateCustomization
    }
}

export namespace emails {
    export interface AccountInformationProps {
        changedByName?: string
        changedByEmail?: string
        updates: {
            value: "name" | "email" | "role" | "password" | "deactivation" | "reactivation"
            originalValue: string | null
            newValue: string | null
        }[] | null
    }

    export interface ActivityUpdates {
        label: string
        originalValue: string | null
        newValue: string
    }

    export interface AppPermissionsReviewProps {
        email?: string
        name?: string
    }

    export interface AssignedTicketNotificationProps {
        ticketNumber: string
        ticketTitle: string
        createdOn: string
        updatedOn: string
        editorName: string
        editorId: string
        updateType: AssignmentUpdateType
        currentAssignment: string
        previousAssignment: string | null
        userName: string
    }

    export type AssignmentUpdateType = "added" | "removed" | "created" | "unchanged" | "ticketSurvey" | "ticketSurveyResults"

    export interface CategoryAssignmentProps {
        userUpdate: AssignmentUpdateType
        categoryName: string
        categoryDescription?: string
        marketCenterName?: string
        marketCenterId?: string
        userName: string
        editorName: string
        editorEmail: string
    }

    export interface CreatedTicketNotificationProps {
        ticketNumber: string
        ticketTitle: string
        creatorName: string
        creatorId: string
        createdOn: string
        dueDate?: string
        assigneeId?: string
        assigneeName?: string
        userName: string
    }

    export interface MarketCenterAssignmentProps {
        userUpdate: AssignmentUpdateType
        marketCenterName?: string
        marketCenterId?: string
        userName: string
        editorName: string
        editorEmail: string
    }

    export interface NewCommentNotificationProps {
        ticketNumber: string
        ticketTitle: string
        createdOn: string
        commenterName: string
        commenterId: string
        comment: string
        isInternal: boolean
        userName: string
    }

    export interface NewUserInvitationProps {
        newUserName: string
        newUserEmail: string
        newUserRole: "AGENT" | "STAFF" | "STAFF_LEADER" | "ADMIN"
        newUserMarketCenter: string | null
        inviterName: string
        inviterEmail: string
    }

    export interface SurveyResultsProps {
        ticketNumber: string
        ticketTitle: string
        staffName: string
        userName: string
    }

    export interface TicketSurveyProps {
        ticketNumber: string
        ticketTitle: string
        surveyorName: string
    }

    export interface UpdatedTicketProps {
        ticketNumber: string
        ticketTitle: string
        createdOn: string
        updatedOn: string
        editorName: string
        editorId: string
        changedDetails: ActivityUpdates[] | string
        userName: string
    }
}

export namespace history {
    export interface GetMarketCenterHistoryRequest {
        orderBy: "asc" | "desc"
        limit?: number
        offset?: number
    }

    export interface GetMarketCenterHistoryResponse {
        marketCenterHistory: marketCenters.MarketCenterHistory[]
        total: number
    }

    export interface GetUserHistoryRequest {
        orderBy: "asc" | "desc"
        limit?: number
        offset?: number
    }

    export interface GetUserHistoryResponse {
        userHistory: user.UserHistory[]
        total: number
    }

    export interface GetUserTicketHistoryRequest {
        orderBy: string
        limit?: number
        offset?: number
    }

    export interface GetUserTicketHistoryResponse {
        ticketHistory: ticket.TicketHistory[]
        total: number
    }
}

export namespace notification_preferences {
    export interface CreateMCNotificationPreferencesResponse {
        success: boolean
    }

    export interface GetMarketCenterNotificationPreferencesResponse {
        notificationPreferences: settings.MarketCenterNotificationPreferences[]
    }

    export interface UpdateMCNotificationPreferencesRequest {
        updatedPreferences: settings.MarketCenterNotificationPreferences[]
    }

    export interface UpdateMCNotificationPreferencesResponse {
        success: boolean
    }
}

export namespace notificationPreferences {
    export interface ResetNotificationsResponse {
        reset: boolean
    }

    export interface UpdateNotificationsRequest {
        /**
         * userSettingsId: string;
         */
        notificationPreferences: user.NotificationPreferences[]
    }

    export interface UpdateNotificationsResponse {
        updated: boolean
    }
}

export namespace templates {
    export type CustomizableTemplateType = "ticket_created" | "ticket_updated" | "ticket_assignment" | "new_comments" | "market_center_assignment" | "category_assignment" | "ticket_survey" | "ticket_survey_results"

    export interface DefaultEmailTemplate {
        subject: string
        greeting: string
        mainMessage: string
        buttonText: string
        visibleFields: string[]
    }

    export interface DefaultInAppTemplate {
        title: string
        body: string
    }

    export interface DeleteTemplatesResponse {
        success: boolean
    }

    export interface EmailTemplateCustomization {
        id: string
        marketCenterId: string
        templateType: CustomizableTemplateType
        /**
         * Customizable content sections
         */
        subject: string

        greeting: string
        mainMessage: string
        buttonText: string | null
        visibleFields: string[]
        /**
         * Metadata
         */
        isActive: boolean

        createdAt: string
        updatedAt: string
        createdById: string | null
        updatedById: string | null
    }

    export interface EmailVisibleField {
        key: string
        label: string
        defaultVisible: boolean
    }

    export interface FormatNotificationRequest {
        type: "Ticket Created" | "Ticket Updated" | "Ticket Assignment" | "Mentions" | "New Comments" | "Market Center Assignment" | "Category Assignment" | "Ticket Survey" | "Ticket Survey Results"
        content: notifications.NotificationContent
    }

    export interface FormatNotificationRequest {
        type: "App Permissions" | "Invitation" | "Account Information"
        content: notifications.NotificationContent
    }

    export interface FormatNotificationResponse {
        formattedNotification: notifications.CreateNotificationPayload | null
    }

    export interface FormatNotificationResponse {
        formattedNotification: notifications.CreateNotificationPayload | null
    }

    export interface GetNotificationTemplateResponse {
        notificationTemplate: notifications.NotificationTemplate
    }

    export interface GetTemplatesRequest {
        newTemplate?: {
            name: string
            description?: string
            isActive: boolean
            title: string
            ticketDescription: string
            categoryId?: string
            defaultAssigneeId?: string
            urgency?: ticket.Urgency
            tags?: string[]
            todos?: string[]
        }
    }

    export interface GetTemplatesResponse {
        templates: TicketTemplate[]
    }

    export interface GetTemplatesResponse {
        templates: TicketTemplate[]
    }

    export interface GetTemplatesResponse {
        template: TicketTemplate
    }

    export interface InAppTemplateCustomization {
        id: string
        marketCenterId: string
        templateType: CustomizableTemplateType
        /**
         * Customizable content
         */
        title: string

        body: string
        /**
         * Metadata
         */
        isActive: boolean

        createdAt: string
        updatedAt: string
        createdById: string | null
        updatedById: string | null
    }

    export interface ListNotificationTemplatesRequest {
        templateName?: string
    }

    export interface ListNotificationTemplatesResponse {
        templates: notifications.NotificationTemplate[]
    }

    export interface ResetNotificationTemplateResponse {
        success: boolean
    }

    export interface TemplateStatus {
        templateType: CustomizableTemplateType
        label: string
        hasEmailCustomization: boolean
        hasInAppCustomization: boolean
        emailCustomization: EmailTemplateCustomization | null
        inAppCustomization: InAppTemplateCustomization | null
    }

    export interface TemplateVariable {
        key: string
        label: string
        description: string
        example: string
    }

    export interface TemplateWithDefaults {
        templateType: CustomizableTemplateType
        label: string
        variables: TemplateVariable[]
        /**
         * Email template
         */
        emailDefault: DefaultEmailTemplate

        emailCustomization: EmailTemplateCustomization | null
        emailVisibleFieldOptions: EmailVisibleField[]
        /**
         * In-app template
         */
        inAppDefault: DefaultInAppTemplate

        inAppCustomization: InAppTemplateCustomization | null
    }

    export interface TicketTemplate {
        id: string
        name: string
        description?: string
        isActive: boolean
        title: string
        ticketDescription: string
        categoryId?: string
        assigneeId?: string
        urgency?: ticket.Urgency
        tags?: string[]
        todos?: string[]
        marketCenterId: string
        createdAt: string
        updatedAt: string
        createdById: string | null
        updatedById: string | null
    }

    export interface UpdateNotificationTemplateRequest {
        templateId: string
        subject?: string
        body?: string
        isActive?: boolean
    }

    export interface UpdateNotificationTemplateResponse {
        success: boolean
    }

    export interface UpdateTemplateRequest {
        isActive: boolean
        templateName: string
        templateDescription: string
        selectedMarketCenter: string
        categoryId?: string
        assignedUserId?: string
        urgency: ticket.Urgency
        ticketTitle: string
        ticketTemplateDescription: string
        ticketTemplateTodos: string[]
    }
}

export namespace ticketCategories {
    export interface CreateCategoryRequest {
        marketCenterId: string
        name: string
        description?: string
        defaultAssigneeId?: string
    }

    export interface CreateCategoryResponse {
        category: marketCenters.TicketCategory
    }

    export interface DeleteCategoryResponse {
        deleted: boolean
    }

    export interface ListCategoriesRequest {
        id?: string
        marketCenterId?: string
    }

    export interface ListCategoriesResponse {
        categories: marketCenters.TicketCategory[]
    }

    export interface UpdateCategoryRequest {
        name?: string
        description?: string
        defaultAssigneeId?: string
    }

    export interface UpdateCategoryResponse {
        category: marketCenters.TicketCategory
        usersToNotify: notifications.UsersToNotify[]
    }
}



function encodeQuery(parts: Record<string, string | string[]>): string {
    const pairs: string[] = []
    for (const key in parts) {
        const val = (Array.isArray(parts[key]) ?  parts[key] : [parts[key]]) as string[]
        for (const v of val) {
            pairs.push(`${key}=${encodeURIComponent(v)}`)
        }
    }
    return pairs.join("&")
}

// makeRecord takes a record and strips any undefined values from it,
// and returns the same record with a narrower type.
// @ts-ignore - TS ignore because makeRecord is not always used
function makeRecord<K extends string | number | symbol, V>(record: Record<K, V | undefined>): Record<K, V> {
    for (const key in record) {
        if (record[key] === undefined) {
            delete record[key]
        }
    }
    return record as Record<K, V>
}

function encodeWebSocketHeaders(headers: Record<string, string>) {
    // url safe, no pad
    const base64encoded = btoa(JSON.stringify(headers))
      .replaceAll("=", "")
      .replaceAll("+", "-")
      .replaceAll("/", "_");
    return "encore.dev.headers." + base64encoded;
}

class WebSocketConnection {
    public ws: WebSocket;

    private hasUpdateHandlers: (() => void)[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        let protocols = ["encore-ws"];
        if (headers) {
            protocols.push(encodeWebSocketHeaders(headers))
        }

        this.ws = new WebSocket(url, protocols)

        this.on("error", () => {
            this.resolveHasUpdateHandlers();
        });

        this.on("close", () => {
            this.resolveHasUpdateHandlers();
        });
    }

    resolveHasUpdateHandlers() {
        const handlers = this.hasUpdateHandlers;
        this.hasUpdateHandlers = [];

        for (const handler of handlers) {
            handler()
        }
    }

    async hasUpdate() {
        // await until a new message have been received, or the socket is closed
        await new Promise((resolve) => {
            this.hasUpdateHandlers.push(() => resolve(null))
        });
    }

    on(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.addEventListener(type, handler);
    }

    off(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.removeEventListener(type, handler);
    }

    close() {
        this.ws.close();
    }
}

export class StreamInOut<Request, Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamIn<Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamOut<Request, Response> {
    public socket: WebSocketConnection;
    private responseValue: Promise<Response>;

    constructor(url: string, headers?: Record<string, string>) {
        let responseResolver: (_: any) => void;
        this.responseValue = new Promise((resolve) => responseResolver = resolve);

        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            responseResolver(JSON.parse(event.data))
        });
    }

    async response(): Promise<Response> {
        return this.responseValue;
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }
}
// CallParameters is the type of the parameters to a method call, but require headers to be a Record type
type CallParameters = Omit<RequestInit, "method" | "body" | "headers"> & {
    /** Headers to be sent with the request */
    headers?: Record<string, string>

    /** Query parameters to be sent with the request */
    query?: Record<string, string | string[]>
}

// AuthDataGenerator is a function that returns a new instance of the authentication data required by this API
export type AuthDataGenerator = () =>
  | auth.AuthParams
  | Promise<auth.AuthParams | undefined>
  | undefined;

// A fetcher is the prototype for the inbuilt Fetch function
export type Fetcher = typeof fetch;

const boundFetch = fetch.bind(this);

class BaseClient {
    readonly baseURL: string
    readonly fetcher: Fetcher
    readonly headers: Record<string, string>
    readonly requestInit: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }
    readonly authGenerator?: AuthDataGenerator

    constructor(baseURL: string, options: ClientOptions) {
        this.baseURL = baseURL
        this.headers = {}

        // Add User-Agent header if the script is running in the server
        // because browsers do not allow setting User-Agent headers to requests
        if (!BROWSER) {
            this.headers["User-Agent"] = "conductor-ee92-Generated-TS-Client (Encore/v1.53.8)";
        }

        this.requestInit = options.requestInit ?? {};

        // Setup what fetch function we'll be using in the base client
        if (options.fetcher !== undefined) {
            this.fetcher = options.fetcher
        } else {
            this.fetcher = boundFetch
        }

        // Setup an authentication data generator using the auth data token option
        if (options.auth !== undefined) {
            const auth = options.auth
            if (typeof auth === "function") {
                this.authGenerator = auth
            } else {
                this.authGenerator = () => auth
            }
        }
    }

    async getAuthData(): Promise<CallParameters | undefined> {
        let authData: auth.AuthParams | undefined;

        // If authorization data generator is present, call it and add the returned data to the request
        if (this.authGenerator) {
            const mayBePromise = this.authGenerator();
            if (mayBePromise instanceof Promise) {
                authData = await mayBePromise;
            } else {
                authData = mayBePromise;
            }
        }

        if (authData) {
            const data: CallParameters = {};

            data.headers = makeRecord<string, string>({
                authorization: authData.authorization,
            });

            return data;
        }

        return undefined;
    }

    // createStreamInOut sets up a stream to a streaming API endpoint.
    async createStreamInOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamInOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamInOut(this.baseURL + path + queryString, headers);
    }

    // createStreamIn sets up a stream to a streaming API endpoint.
    async createStreamIn<Response>(path: string, params?: CallParameters): Promise<StreamIn<Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamIn(this.baseURL + path + queryString, headers);
    }

    // createStreamOut sets up a stream to a streaming API endpoint.
    async createStreamOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamOut(this.baseURL + path + queryString, headers);
    }

    // callTypedAPI makes an API call, defaulting content type to "application/json"
    public async callTypedAPI(method: string, path: string, body?: RequestInit["body"], params?: CallParameters): Promise<Response> {
        return this.callAPI(method, path, body, {
            ...params,
            headers: { "Content-Type": "application/json", ...params?.headers }
        });
    }

    // callAPI is used by each generated API method to actually make the request
    public async callAPI(method: string, path: string, body?: RequestInit["body"], params?: CallParameters): Promise<Response> {
        let { query, headers, ...rest } = params ?? {}
        const init = {
            ...this.requestInit,
            ...rest,
            method,
            body: body ?? null,
        }

        // Merge our headers with any predefined headers
        init.headers = {...this.headers, ...init.headers, ...headers}

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                init.headers = {...init.headers, ...authData.headers};
            }
        }

        // Make the actual request
        const queryString = query ? '?' + encodeQuery(query) : ''
        const response = await this.fetcher(this.baseURL+path+queryString, init)

        // handle any error responses
        if (!response.ok) {
            // try and get the error message from the response body
            let body: APIErrorResponse = { code: ErrCode.Unknown, message: `request failed: status ${response.status}` }

            // if we can get the structured error we should, otherwise give a best effort
            try {
                const text = await response.text()

                try {
                    const jsonBody = JSON.parse(text)
                    if (isAPIErrorResponse(jsonBody)) {
                        body = jsonBody
                    } else {
                        body.message += ": " + JSON.stringify(jsonBody)
                    }
                } catch {
                    body.message += ": " + text
                }
            } catch (e) {
                // otherwise we just append the text to the error message
                body.message += ": " + String(e)
            }

            throw new APIError(response.status, body)
        }

        return response
    }
}

/**
 * APIErrorDetails represents the response from an Encore API in the case of an error
 */
interface APIErrorResponse {
    code: ErrCode
    message: string
    details?: any
}

function isAPIErrorResponse(err: any): err is APIErrorResponse {
    return (
        err !== undefined && err !== null &&
        isErrCode(err.code) &&
        typeof(err.message) === "string" &&
        (err.details === undefined || err.details === null || typeof(err.details) === "object")
    )
}

function isErrCode(code: any): code is ErrCode {
    return code !== undefined && Object.values(ErrCode).includes(code)
}

/**
 * APIError represents a structured error as returned from an Encore application.
 */
export class APIError extends Error {
    /**
     * The HTTP status code associated with the error.
     */
    public readonly status: number

    /**
     * The Encore error code
     */
    public readonly code: ErrCode

    /**
     * The error details
     */
    public readonly details?: any

    constructor(status: number, response: APIErrorResponse) {
        // extending errors causes issues after you construct them, unless you apply the following fixes
        super(response.message);

        // set error name as constructor name, make it not enumerable to keep native Error behavior
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/new.target#new.target_in_constructors
        Object.defineProperty(this, 'name', {
            value:        'APIError',
            enumerable:   false,
            configurable: true,
        })

        // fix the prototype chain
        if ((Object as any).setPrototypeOf == undefined) {
            (this as any).__proto__ = APIError.prototype
        } else {
            Object.setPrototypeOf(this, APIError.prototype);
        }

        // capture a stack trace
        if ((Error as any).captureStackTrace !== undefined) {
            (Error as any).captureStackTrace(this, this.constructor);
        }

        this.status = status
        this.code = response.code
        this.details = response.details
    }
}

/**
 * Typeguard allowing use of an APIError's fields'
 */
export function isAPIError(err: any): err is APIError {
    return err instanceof APIError;
}

export enum ErrCode {
    /**
     * OK indicates the operation was successful.
     */
    OK = "ok",

    /**
     * Canceled indicates the operation was canceled (typically by the caller).
     *
     * Encore will generate this error code when cancellation is requested.
     */
    Canceled = "canceled",

    /**
     * Unknown error. An example of where this error may be returned is
     * if a Status value received from another address space belongs to
     * an error-space that is not known in this address space. Also
     * errors raised by APIs that do not return enough error information
     * may be converted to this error.
     *
     * Encore will generate this error code in the above two mentioned cases.
     */
    Unknown = "unknown",

    /**
     * InvalidArgument indicates client specified an invalid argument.
     * Note that this differs from FailedPrecondition. It indicates arguments
     * that are problematic regardless of the state of the system
     * (e.g., a malformed file name).
     *
     * This error code will not be generated by the gRPC framework.
     */
    InvalidArgument = "invalid_argument",

    /**
     * DeadlineExceeded means operation expired before completion.
     * For operations that change the state of the system, this error may be
     * returned even if the operation has completed successfully. For
     * example, a successful response from a server could have been delayed
     * long enough for the deadline to expire.
     *
     * The gRPC framework will generate this error code when the deadline is
     * exceeded.
     */
    DeadlineExceeded = "deadline_exceeded",

    /**
     * NotFound means some requested entity (e.g., file or directory) was
     * not found.
     *
     * This error code will not be generated by the gRPC framework.
     */
    NotFound = "not_found",

    /**
     * AlreadyExists means an attempt to create an entity failed because one
     * already exists.
     *
     * This error code will not be generated by the gRPC framework.
     */
    AlreadyExists = "already_exists",

    /**
     * PermissionDenied indicates the caller does not have permission to
     * execute the specified operation. It must not be used for rejections
     * caused by exhausting some resource (use ResourceExhausted
     * instead for those errors). It must not be
     * used if the caller cannot be identified (use Unauthenticated
     * instead for those errors).
     *
     * This error code will not be generated by the gRPC core framework,
     * but expect authentication middleware to use it.
     */
    PermissionDenied = "permission_denied",

    /**
     * ResourceExhausted indicates some resource has been exhausted, perhaps
     * a per-user quota, or perhaps the entire file system is out of space.
     *
     * This error code will be generated by the gRPC framework in
     * out-of-memory and server overload situations, or when a message is
     * larger than the configured maximum size.
     */
    ResourceExhausted = "resource_exhausted",

    /**
     * FailedPrecondition indicates operation was rejected because the
     * system is not in a state required for the operation's execution.
     * For example, directory to be deleted may be non-empty, an rmdir
     * operation is applied to a non-directory, etc.
     *
     * A litmus test that may help a service implementor in deciding
     * between FailedPrecondition, Aborted, and Unavailable:
     *  (a) Use Unavailable if the client can retry just the failing call.
     *  (b) Use Aborted if the client should retry at a higher-level
     *      (e.g., restarting a read-modify-write sequence).
     *  (c) Use FailedPrecondition if the client should not retry until
     *      the system state has been explicitly fixed. E.g., if an "rmdir"
     *      fails because the directory is non-empty, FailedPrecondition
     *      should be returned since the client should not retry unless
     *      they have first fixed up the directory by deleting files from it.
     *  (d) Use FailedPrecondition if the client performs conditional
     *      REST Get/Update/Delete on a resource and the resource on the
     *      server does not match the condition. E.g., conflicting
     *      read-modify-write on the same resource.
     *
     * This error code will not be generated by the gRPC framework.
     */
    FailedPrecondition = "failed_precondition",

    /**
     * Aborted indicates the operation was aborted, typically due to a
     * concurrency issue like sequencer check failures, transaction aborts,
     * etc.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     */
    Aborted = "aborted",

    /**
     * OutOfRange means operation was attempted past the valid range.
     * E.g., seeking or reading past end of file.
     *
     * Unlike InvalidArgument, this error indicates a problem that may
     * be fixed if the system state changes. For example, a 32-bit file
     * system will generate InvalidArgument if asked to read at an
     * offset that is not in the range [0,2^32-1], but it will generate
     * OutOfRange if asked to read from an offset past the current
     * file size.
     *
     * There is a fair bit of overlap between FailedPrecondition and
     * OutOfRange. We recommend using OutOfRange (the more specific
     * error) when it applies so that callers who are iterating through
     * a space can easily look for an OutOfRange error to detect when
     * they are done.
     *
     * This error code will not be generated by the gRPC framework.
     */
    OutOfRange = "out_of_range",

    /**
     * Unimplemented indicates operation is not implemented or not
     * supported/enabled in this service.
     *
     * This error code will be generated by the gRPC framework. Most
     * commonly, you will see this error code when a method implementation
     * is missing on the server. It can also be generated for unknown
     * compression algorithms or a disagreement as to whether an RPC should
     * be streaming.
     */
    Unimplemented = "unimplemented",

    /**
     * Internal errors. Means some invariants expected by underlying
     * system has been broken. If you see one of these errors,
     * something is very broken.
     *
     * This error code will be generated by the gRPC framework in several
     * internal error conditions.
     */
    Internal = "internal",

    /**
     * Unavailable indicates the service is currently unavailable.
     * This is a most likely a transient condition and may be corrected
     * by retrying with a backoff. Note that it is not always safe to retry
     * non-idempotent operations.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     *
     * This error code will be generated by the gRPC framework during
     * abrupt shutdown of a server process or network connection.
     */
    Unavailable = "unavailable",

    /**
     * DataLoss indicates unrecoverable data loss or corruption.
     *
     * This error code will not be generated by the gRPC framework.
     */
    DataLoss = "data_loss",

    /**
     * Unauthenticated indicates the request does not have valid
     * authentication credentials for the operation.
     *
     * The gRPC framework will generate this error code when the
     * authentication metadata is invalid or a Credentials callback fails,
     * but also expect authentication middleware to generate it.
     */
    Unauthenticated = "unauthenticated",
}
